# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Read this first

These constraints change how you work here before you touch anything:

- **There are two test suites, and between them they still cover little.** `npm test` runs Vitest (jsdom) over 99 tests in nine files beside what they cover — `src/lib/repx.test.ts` (13), `sourceRect.test.ts` (9), `designerBridge.test.ts` (6), `attachments.test.ts` (9), `reportConfigStore.test.ts` (9), `panelSize.test.ts` (13), `announcements.test.ts` (7), `router.test.ts` (17) and `src/services/geminiService.test.ts` (16) — covering the pure helpers in `src/lib`, the config-persistence allowlist in `reportConfigStore.ts`, the stream scanner in `geminiService.ts`, and — the one file that is not a pure helper — the `history`/`location` handling in `router.ts`, which needs the jsdom environment rather than merely tolerating it. Each was chosen because it encodes an invariant that fails *plausibly* rather than loudly. `npm run test:rules` runs 29 tests in `tests/firestore.rules.test.ts` against the Firestore emulator — account isolation, document shape, and the key vault. **Everything else is still unenforced**: no component test, no render test, and nothing touching `App.tsx`'s stateful logic. Where this document calls something an invariant and it is not in one of those files, you check it by hand or it ships broken. (This bullet is the **only** place in the repo the counts are written down. The Commands table below and `README.md` both describe the suites without numbering them, on purpose — three copies of "38" drifted apart once already. If you add a test, this is the line to update, and it should stay the only one. The per-file numbers sum to the total, so adding a case changes **two** numbers here, not one — and a mismatch between them is the cheapest possible signal that this line went stale.)
- **`npm run test:rules` needs Java** (the Firestore emulator is a JVM process). A Temurin JRE 21 is installed at `%LOCALAPPDATA%\Programs\Temurin\jdk-21.0.12+8-jre` with `JAVA_HOME` and `PATH` set at user scope — no admin was required, and nothing was installed system-wide. On a machine without it, that script fails with a Java-not-found error while `npm test` still works.
- **The Node floor is 20, nothing enforces it, and it is not a round-number preference.** `package.json` has **no `engines` field**, so npm will not warn on an old runtime — the failure arrives later as a syntax or API error from a dependency. The floor is set by `vitest` (`^20 || ^22 || >=24`) and `firebase-tools` (`>=20`); `vite` alone would still accept 18. Note vitest's range is the even LTS lines *plus* `>=24`, so **Node 21 and 23 fall outside it** and ">=20" is the wrong mental model. Developed and verified on **Node 24.18.1 / npm 11.16.0**. `README.md` states the same floor for contributors, so changing it is a change to two files.
- **`tsconfig.json` does not enable `strict`**, so a clean `tsc` proves much less than it looks like. Type errors that matter will not surface on their own — read carefully rather than trusting the exit code. The baseline *is* clean, so anything `tsc` prints is yours.
- **Never edit these files with PowerShell `Get-Content` / `Set-Content`.** Windows PowerShell 5.1 decodes with the ANSI codepage, so a read-modify-write round-trip turns every UTF-8 character into mojibake — and the source is full of em dashes, `•` and `…`, including in user-visible strings. This happened: bulk edits via `Get-Content` corrupted 2,768 sequences in `App.tsx`, which reached the UI as *"can run without it ÃƒÆ'Ã†'..."* in the API-key banner and the status bar. It compiles and every test passes, because nothing checks string contents — it is only visible by looking at the rendered page. Use the Edit tool, or `[IO.File]::ReadAllText/WriteAllText` with an explicit `UTF8Encoding($false)`, which are UTF-8-correct. To check: `[regex]::Matches($text,'Ã|Â|â€').Count` should be 0 in every source file — **with one standing exception: this file scores 8**, from the corrupted example quoted just above and the check's own regex under *Commands*. Those are literals, not damage; every other file must be 0, so run the sweep over `src/`, `docs/`, `tests/`, `scripts/`, `tools/` **and the root-level files — `server.ts`, the three configs, `README.md`, `index.html`, `firestore.rules`, `.gitignore`, `.env.example`** — and leave `CLAUDE.md` out of it or expect that number. The root is not a formality: `server.ts` and `.gitignore` both carry em dashes inside comments, and a sweep scoped to the three source directories would score them 0 by never opening them. **The same trap catches a `.ps1` you write to do the editing, and `WriteAllText` does not save you from it:** PowerShell 5.1 decodes a BOM-less script file as ANSI as well, so non-ASCII prose sitting in a string literal *inside* the script is already mojibake before the correct writer emits it. Observed on 2026-08-13 while splitting this file — the moved text came through byte-perfect and every em dash in the new prose the script carried was corrupted. Keep generator scripts pure ASCII and have them read any prose they emit from a UTF-8 file written with the Write tool.
- **The working tree is a git repository**, initialised on 2026-08-09. `git diff` and `git log` work; there is still no remote, so nothing has been pushed. Read `git log` rather than trusting a commit count written here.
- **Write commit messages to a file and use `git commit -F <file>`, never `-m` with a here-string.** The commits here have long explanatory bodies, and PowerShell 5.1 re-parses a string containing `"` before handing it to a native executable — so a `@'…'@` here-string carrying quoted text arrives at `git` split into dozens of arguments. Observed: one message produced ~45 `error: pathspec '…' did not match any file(s)` lines and committed nothing. Write the message with the Write tool (UTF-8, and the same mojibake rule as above applies to the message file), then `git commit -F`. If you must use `-m`, the message has to contain no double quotes at all.
- **`README.md` exists and is public-facing** — it is the open-source front door, not a duplicate of this file. It carries setup, a Firebase bring-your-own-project walkthrough, and the honest limitations list. It restates a handful of facts that also live here (the key story, the tree layout, the script list), so a change to one of those is a change to two files. It deliberately carries no test counts — see the bullet above.
- **There is no `LICENSE` file, and `package.json` has no `license` field.** Default copyright applies until one is added — whatever the open-source intent, nobody currently has the right to use, modify or distribute this code. **Do not add a license header, an SPDX identifier, or a badge to any file on the assumption that it will be MIT**; the choice has not been made, and a header asserting one is worse than no header at all. `README.md` says the same under *License*, so actually declaring one is a change to three files (`LICENSE`, `package.json`, `README.md`).
- **The app owns no Gemini API key.** Each user supplies their own at runtime and the browser calls Google directly with it. This is the correction to a real leak incident — see *The Gemini call runs in the browser* in [`docs/notes/gemini.md`](docs/notes/gemini.md).

Stack: React 19, Vite 6, Tailwind v4, TypeScript, Express (dev *and* prod server), Firebase Auth + Firestore, Gemini via `@google/genai`. Entry points are `server.ts`, `src/main.tsx` → `src/App.tsx`, and `src/services/geminiService.ts`.

Most of the reasoning behind this codebase is **not in this file**. It lives in four notes under `docs/notes/`, split out because carrying all of it in every session cost roughly 24k tokens to answer a question about one area. **They are not loaded automatically, and the one-line summaries below are not a substitute for them.** Open the note that owns what you are touching before you change it: each records the incidents that produced the current shape, and the cost of skipping it is re-breaking something that was already fixed once.

| If you are touching… | Read |
|---|---|
| the API key, `.env`, `envPrefix`, anything server-side | [`gemini.md`](docs/notes/gemini.md) — *The Gemini call runs in the browser* + *Where the user's key lives* |
| a model id, a 404/429/503, `thinkingBudget` | [`gemini.md`](docs/notes/gemini.md) — *The model is auto-detected* + *Chat and report generation are separate paths* |
| the prompt, `layout` schema, mockup rendering, image crops | [`gemini.md`](docs/notes/gemini.md) — *`geminiService.ts` is a single mega-prompt* |
| file upload, PDF handling, `.repx` intake | [`app-shell.md`](docs/notes/app-shell.md) — File intake |
| the workspace shell, any `wb-` class, `src/workspace.css` | [`app-shell.md`](docs/notes/app-shell.md) (the 2026-08-12 port) + [`styling.md`](docs/notes/styling.md) (the artifact token aliases) |
| dragging or collapsing the review column, `src/lib/panelSize.ts` | [`app-shell.md`](docs/notes/app-shell.md) — *The review column is resizable and collapsible*, the first deliberate departure from the port |
| a route, a deep link, `document.title` | [`app-shell.md`](docs/notes/app-shell.md) — *Routing is real paths* |
| the progress bar, pause/stop, streaming | [`app-shell.md`](docs/notes/app-shell.md) (Progress) + [`gemini.md`](docs/notes/gemini.md) (*Chat and report generation are separate paths*) |
| saving/loading reports, sign-in, `firestore.rules` | [`persistence.md`](docs/notes/persistence.md) |
| `firebase-applet-config.json`, `databaseId`, the `AIzaSy…` string in the repo root | [`persistence.md`](docs/notes/persistence.md) — its `firestoreDatabaseId` must match `databaseId` in `firebase.json` or you silently talk to the wrong database. That key is a **public project identifier and stays committed** — it is not the class of value a Gemini key is; `README.md` says so too |
| the account menu, `AccountDialog.tsx`, `UserAvatar.tsx` | [`app-shell.md`](docs/notes/app-shell.md) — *Account management lives in `AccountDialog.tsx`* + [`persistence.md`](docs/notes/persistence.md) for what deleting an account has to remove |
| a marketing page (`LandingPage`, `LoginPage`, `DocsPage`, `FeaturesPage`, `ContactPage`), `SiteHeader`/`SiteFooter`/`MobileNav`, anything under `src/components/landing/` | [`app-shell.md`](docs/notes/app-shell.md) (the pages and their shared chrome) + [`styling.md`](docs/notes/styling.md) (`Logo`, the `.landing` scope) |
| `/terms`, `/privacy`, `LegalPage.tsx` | [`app-shell.md`](docs/notes/app-shell.md) — the sign-up form links them, so they are not optional pages |
| the home page announcement dock, `src/lib/announcements.ts` | [`app-shell.md`](docs/notes/app-shell.md) — the 2026-08-14 dock |
| theme, tokens, animation, the logo, `src/index.css` | [`styling.md`](docs/notes/styling.md) — and the landing palette/typography mirrors the YAML frontmatter of [`docs/design/DESIGN.md`](docs/design/DESIGN.md) **by hand**, so changing one is a change to both |
| the **Open in designer** button, `src/lib/designerBridge.ts`, anything under `tools/` | [`tools/RepxDesigner/README.md`](tools/RepxDesigner/README.md) — the one doc that lives **outside** `docs/notes/`, so a grep of the notes will not find it |

If what you are changing is not in that table, grep the notes before concluding it is undocumented — they are prose, and this index is deliberately shorter than they are. Grep `tools/RepxDesigner/README.md` too; it is the fifth note in everything but location.

**When you add to the record, it goes in the note, not here.** A new incident, a new invariant, a new reason something is the way it is — all of that is area-specific and belongs in the note that owns the area. This file grows only for what every session needs regardless of what it is touching: the constraints in *Read this first*, the commands, the checks below. Appending area-specific reasoning to the index is precisely how it reached 94,616 characters before the split on 2026-08-13, and it is the one habit that will undo that.

## Project

**Forma** — an AI-powered DevExpress report designer. The user uploads an image, PDF, or `.repx` file; Gemini analyzes it and returns three artifacts in one structured JSON response: a markdown spec, a `layout` JSON used to draw an in-browser mockup, and `repxContent` — valid DevExpress `XtraReportsLayoutSerializer` XML that downloads as a `.repx` file. See `docs/PRD.md` for the product spec — it is the single copy, kept current against the code, including its "not yet exposed in the UI" note (RTL, stored-procedure name, data-binding schema), its §5 record of the resolved key exposures, and its §6 statement that **moving generation server-side is explicitly out of scope**, not future work. (A stale `public/PRD.pdf` used to shadow it; it was unreferenced and has been deleted.) `docs/PRD.md` was rewritten on 2026-08-02 to catch up with the bring-your-own-key rework — it had still been describing a `localStorage`-stored key, a model dropdown, an application-owned quota, and a server-side generation endpoint. If you change any of those areas, update it in the same pass.

`metadata.json` was AI Studio applet metadata and the last place the pre-Forma name "DevExpress Report AI Designer" survived. Nothing read it, so it is parked at `_not_required/metadata.json`. `package.json`'s `"name": "react-example"` is likewise a leftover scaffold value, not a product name.

**`_not_required/` holds four parked files, and it is tracked, so a grep reaches all of them.** Besides `metadata.json` and the `xcode_project_setup` skill it carries `firebase-blueprint.json` and `public/LoginPage/Contact&InquirePage/DESIGN.md`. That last one is a decoy: the live design source is [`docs/design/DESIGN.md`](docs/design/DESIGN.md), whose YAML frontmatter the landing palette and typography mirror **by hand**. Nothing under `_not_required/` is read, built, or imported — if a search lands you there, the answer is somewhere else.

## Commands

```powershell
npm run dev          # tsx server.ts -> Express + Vite middleware on http://localhost:3000
npm run dev:log      # same server, output also mirrored to dev-server.log (gitignored)
npm run build        # vite build (client) + esbuild (server) -> dist/
npm run build:server # server bundle only -> dist/server.cjs
npm start            # node dist/server.cjs -- serves dist/ statically, no Vite
npm run lint         # tsc --noEmit  (there is still no ESLint config)
npm test             # vitest run -- the pure helpers in src/lib + geminiService
npm run test:watch   # the same suite in watch mode
npm run test:rules   # security-rule tests against the Firestore emulator (needs Java)
npm run clean        # removes dist/ (node fs.rmSync, works on Windows and POSIX)
npm run preview      # vite preview against dist/
```

**These four commands are the whole of what a machine checks here.** Nothing else in this document is enforced — every other invariant it records is verified by reading or by looking at the rendered page:

```powershell
npm run lint                                            # tsc --noEmit; the baseline is clean, so any output is yours
npx tsc --noEmit --noUnusedLocals --noUnusedParameters  # dead code; also clean, so treat any hit as newly introduced
npm test                                                # the pure helpers
npm run test:rules                                      # firestore.rules, via the emulator (needs Java)
```

Add the encoding check to that list for any file a shell may have rewritten: `[regex]::Matches([IO.File]::ReadAllText($path),'Ã|Â|â€').Count` must be `0` — **except on `CLAUDE.md` itself, which contains the pattern as prose and scores 8**; sweep `src/`, `docs/`, `tests/`, `scripts/`, `tools/` and the root-level `*.ts` / `*.md` / `.gitignore` / `.env.example` / `index.html` / `firestore.rules`, and skip this file rather than reading its hits as corruption. Each of the five is explained where it belongs — the two suites, the `strict` caveat and the mojibake incident in *Read this first*, the unused sweep under *`src/App.tsx` is the whole app* in [`docs/notes/app-shell.md`](docs/notes/app-shell.md). This block is a runnable summary of those, deliberately carrying no test counts and no second copy of the reasoning.

- **Never run bare `vite dev`.** The dev server is `server.ts`; Vite runs in middleware mode inside it.
- The dev server belongs in the user's own terminal, not an agent background shell — it is long-lived and browser-facing. When you need to see its output, ask the user to run `npm run dev:log` and read `dev-server.log`; only take the port yourself for a short, self-contained check, and free it afterwards.
- **`RepxDesigner.exe --serve` belongs in the user's terminal for the same reason, and a stronger one.** A process paints on the desktop of the session that created it, so an instance you start from an agent shell opens the designer where nobody can see it — and reports success all the way back to the browser. Ask the user to double-click it. See *Start it yourself, from your own desktop* in [`tools/RepxDesigner/README.md`](tools/RepxDesigner/README.md).
- `scripts/build-server.mjs` bundles `server.ts` with esbuild, keeping `node_modules` external and baking in `NODE_ENV=production` — so the started server takes the static-file branch and the Vite import is dead code. `npm start` therefore requires `npm run build` first, and must run from the repo root (the static path is `process.cwd()/dist`).
- **`.env` does not contain a Gemini key and must not.** It holds `APP_URL` plus the optional `VITE_FORMA_MOCK`, and `server.ts` loads it via `import "dotenv/config"` — but the server has no key-bearing routes left, and `envPrefix: ['VITE_']` means a `GEMINI_API_KEY` set there reaches neither the server code nor the browser. Nothing is inlined into the client bundle. `.env*` is gitignored except `.env.example`, whose comments say the same thing.
- Run a single test file with `npx vitest run src/lib/repx.test.ts`, or a single case with `-t "<name>"`. Narrowing to one file buys clarity, not time: jsdom startup dominates, so a single 9-test file takes ~30s against ~34s for the whole suite. **`--reporter=basic` no longer exists** — Vitest 4 removed it, and an unknown reporter name is treated as a module to import, so the failure is a `Failed to load custom Reporter from basic` / `Failed to load url basic (resolved id: basic)` stack trace with Vite frames in it, which reads like a broken config rather than a bad flag value. `vitest.config.ts` already sets `reporters: 'default'`; pass `--reporter=dot` for the terse output `basic` used to give. Verified on vitest 4.1.10. `vitest.config.ts` is deliberately separate from `vite.config.ts` — that one carries the "Do not modify" HMR block and is loaded by the dev server. Tests live beside what they test as `*.test.ts`; the config only picks up `src/**/*.test.ts`. See "Read this first" for what the suite does *not* cover.
- **There are three Vitest/Vite configs, and the rules suite uses its own.** `npm run test:rules` is `firebase emulators:exec --only firestore "vitest run --config vitest.rules.config.ts"` — the emulator wrapper is what supplies the Firestore instance, so `vitest run --config vitest.rules.config.ts` on its own has nothing to talk to. `tests/firestore.rules.test.ts` sits outside `src/`, which is why the default config never picks it up and `npm test` cannot run it by accident.
- **To run a single rules case, use a space-free regex — do not try to nest quotes.** The `-t` flag has to go *inside* the quoted command the emulator wrapper runs, and PowerShell 5.1 re-parses that string before `firebase` ever sees it, so the obvious `-t \"lets a user read their own report\"` arrives mangled (observed: `Script "… -t " lets a user read their own report\" exited with code 1`). `-t` is a **regex**, so `.` in place of each space sidesteps quoting entirely:

  ```powershell
  npx firebase emulators:exec --only firestore "npx vitest run --config vitest.rules.config.ts -t lets.a.user.read.their.own.report"
  ```

  Verified: `1 passed | 28 skipped`. A distinctive single word (`-t isolation`) works the same way and is shorter.
- `.claude/settings.local.json` carries a permission allowlist covering every command in the two blocks above — `npm run *`, `npm test`, `npm ls *`, `npx tsc *`, `npx vitest *`, `Get-Process`, `New-Item`, plus a handful of one-off probes left over from past sessions — so the documented checks run without a prompt. Note `npm test` needs its own entry: it is not `npm run test`, so `npm run *` does not match it. `dev-server.log` is gitignored by the blanket `*.log`, not by name.
- **`.antigravity/` is editor state, not project data** — a single ~22 MB `.pbtxt`. Nothing reads it and nothing builds from it. It is now explicitly gitignored (it matched no pattern originally, so a first `git add .` would have put 22 MB into every clone forever), but it is still on disk, so a recursive listing or a "what's in this repo" sweep will trip over it. Don't read it, don't index it, and don't count it as source.
- **`.gitignore` carries credential patterns that are a backstop, not permission.** `key.txt`, `*.key`, `*apikey*`, `*api-key*` and `secrets.json` are ignored so a key pasted into the working tree by mistake cannot be committed by a careless `git add .` — the same accident as the `envPrefix` leak, with fewer steps. They do **not** make the repo a place to keep a key: it belongs in the app's Settings dialog, which puts it in `sessionStorage` and never on disk (see *Where the user's key lives* in [`docs/notes/gemini.md`](docs/notes/gemini.md)). Also note `*.log` is what hides `dev-server.log` and `firestore-debug.log`, and `.env*` is ignored **except** `.env.example`.
- **`.firebaserc` is deliberately *not* ignored, and that is the point.** It names the deploy target (`forma-201ba`) and holds no credentials. Ignoring it is exactly how a fresh clone or a CI run deploys to whichever project the CLI last happened to select — see [`docs/notes/persistence.md`](docs/notes/persistence.md) for the time that went wrong. `.firebase/` (deploy caches, emulator exports) **is** ignored.
- `vite.config.ts` gates HMR on `DISABLE_HMR !== 'true'` and carries an explicit *"Do not modify — file watching is disabled to prevent flickering during agent edits"* comment. Leave that block alone. The `watch: { ignored: ['**/tools/**'] }` line beside it is load-bearing too: chokidar throws `EBUSY` on MSBuild's locked `bin/`/`obj/` files and takes the whole dev server down with it.
- Symbol names in this document are stable; line numbers are not. Grep for the identifier rather than jumping to a remembered offset — `src/App.tsx` has already grown past every line number an earlier revision of this file recorded.

## Architecture

Four notes, one per area, under `docs/notes/` — plus the designer companion's own README, listed last because it is the one an area-based search misses. The section headings inside them are unchanged from when they lived here, so a section name quoted in a commit message or anywhere in this file still finds its text with `grep -rn "<name>" docs/notes/`.

| Note | What it owns |
|---|---|
| [`gemini.md`](docs/notes/gemini.md) | The browser-side Gemini call and why it is not on the server; where the user's key lives and how the vault encrypts it; the mega-prompt and the `layout` schema; runtime model detection and the 404/429/503 paths; the chat-vs-generation split and the streaming reply. |
| [`app-shell.md`](docs/notes/app-shell.md) | `src/App.tsx` and everything it still holds: the ported workspace, the marketing pages and their shared chrome, real-path routing, file intake and PDF text extraction, the REPX viewer, streaming progress and cancellation. |
| [`persistence.md`](docs/notes/persistence.md) | Firestore versus `localStorage` and the shapes that diverge between them, the cloud save budget, `firestore.rules`, and the sign-in fallback that was removed for being an authentication bypass. |
| [`tools/RepxDesigner/README.md`](tools/RepxDesigner/README.md) | The Windows companion that opens a generated `.repx` in the real DevExpress designer: the loopback `--serve` listener on `127.0.0.1:7317`, its two guards, the feature-detected **Open in designer** button behind `src/lib/designerBridge.ts`, DevExpress 20.1 version matching, and the MSBuild build. Outside `docs/notes/` because it documents a C# project, not the web app. |
| [`styling.md`](docs/notes/styling.md) | Design tokens and the four-part theme resolution, the `.sheet` / `.landing` scopes, the icon set, the `Logo` component, and the division of labour between CSS helpers and Framer Motion. |

## Firebase skills

`.agents/skills/` contains Firebase's official skill packs — `firebase_basics`, `firebase_auth_basics`, `firebase_firestore`, `firebase_security_rules_auditor`, `firebase_hosting_basics`, `firebase_app_hosting_basics`, plus `firebase_ai_logic_basics`, `firebase_crashlytics`, `firebase_data_connect_basics` and `firebase_remote_config_basics` for products this app does not use. Consult the relevant `SKILL.md` before changing `firestore.rules`, auth flows, or deployment config. (`xcode_project_setup` shipped in the same folder and is irrelevant here; it is parked at `_not_required/.agents/skills/xcode_project_setup/`.)
