# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Read this first

These constraints change how you work here before you touch anything:

- **There are two test suites, and between them they still cover little.** `npm test` runs Vitest over 1,137 tests in 52 files — under the `node` environment by default, with the eight files that genuinely need a DOM opting into jsdom on their own first line (see `vitest.config.ts` for why that split is load-bearing rather than tidiness). `npm run test:rules` runs 70 tests in three files against the Firestore emulator. Between them they cover the pure helpers in `src/lib`, the config-persistence allowlist, the stream scanner, the AES-GCM key vault, the two outbound calls that can hang, the three `src/server` files, `firestore.rules`, and two component render tests. Each was chosen because it encodes an invariant that fails *plausibly* rather than loudly. **Everything else is unenforced** — `App.tsx` is ~4,450 lines with perhaps sixty of them extracted into tested modules, and no other component has a render test. Where this document calls something an invariant and it is not in one of those files, you check it by hand or it ships broken.
- **Recount test numbers by running the suite, never by grepping.** A grep for `\bit\(` answers 1,115 against a real 1,137, because five files generate their cases from a loop; the 22-test shortfall is a constant, and a change in the *gap* is the signal worth reading. [`docs/notes/testing.md`](docs/notes/testing.md) is **the only place in the repo the counts are written down** — the per-file inventory, the counting traps, the three rules files and what `test:coverage` does and does not measure all live there, and adding a test is a change to that note. The *Commands* block below and `README.md` deliberately describe the suites without numbering them; three copies of "38" drifted apart once already.
- **`npm run test:rules` needs Java** (the Firestore emulator is a JVM process). A Temurin JRE 21 is installed at `%LOCALAPPDATA%\Programs\Temurin\jdk-21.0.12+8-jre` with `JAVA_HOME` and `PATH` set at user scope — no admin was required, and nothing was installed system-wide. On a machine without it, that script fails with a Java-not-found error while `npm test` still works.
- **The Node floor is 20, and it is not a round-number preference.** `package.json` declares `"engines": { "node": "^20 || ^22 || >=24" }` as of `493748e` (2026-08-27) — before that there was no field at all, so npm warned about nothing and a wrong runtime arrived later as a syntax or API error from inside a dependency, which reads as a broken repository rather than a bad Node version. Note npm *warns*, it does not refuse: there is no `engine-strict`, so the declaration is a signal, not a gate. The floor is set by `vitest` (`^20 || ^22 || >=24`) and `firebase-tools` (`>=20`); `vite` alone would still accept 18. Note vitest's range is the even LTS lines *plus* `>=24`, so **Node 21 and 23 fall outside it** and ">=20" is the wrong mental model. Developed and verified on **Node 24.18.1 / npm 11.16.0**. `README.md` *and* `CONTRIBUTING.md` both state the same floor for contributors, so changing it is a change to **three** files — this bullet said "two" until 2026-09-01, which is how a rule about drift acquired its own.
- **`tsconfig.json` enables `strict` as of 2026-08-27**, so `npm run lint` now means roughly what it looks like it means — and **the baseline is clean, so anything `tsc` prints is yours.** Two things follow that are easy to undo by accident. `@types/react` is now *declared* rather than inherited from `react-markdown`; do not remove it from `devDependencies` on the grounds that nothing imports it, because without it every `.tsx` file silently degrades to `any` while `tsc` still exits 0 — the same failure `strict` exists to prevent. And the `exclude` block is load-bearing: without it `npm run lint` reads `dist/`, which `allowJs` makes it typecheck, so whether the only linter here passes depends on whether generated code nobody wrote happens to satisfy `strict` and on whether `dist/` exists when you run it. Check with `tsc --noEmit --listFiles` — it should read only this project's own sources, and nothing from `dist`, `tools` or `coverage`. Excluding them also took lint from ~22s to ~9s. **The full reasoning for both flags is in `tsconfig.json`'s own comments**, at more length than any summary here.
- **Never edit these files with PowerShell `Get-Content` / `Set-Content`.** Windows PowerShell 5.1 decodes with the ANSI codepage, so a read-modify-write round-trip turns every UTF-8 character into mojibake — and the source is full of em dashes, `•` and `…`, including in user-visible strings. This happened: bulk edits via `Get-Content` corrupted 2,768 sequences in `App.tsx`, which reached the UI as *"can run without it ÃƒÆ'Ã†'..."* in the API-key banner and the status bar. It compiles and every test passes, because nothing checks string contents — it is only visible by looking at the rendered page. Use the Edit tool, or `[IO.File]::ReadAllText/WriteAllText` with an explicit `UTF8Encoding($false)`, which are UTF-8-correct. To check a single file, `[regex]::Matches($text,'Ã|Â|â€').Count` should be 0 — **with one standing exception: this file scores 8**, five of them in this bullet (the corrupted example just above, plus that regex, which contributes one hit per alternative) and three in the runnable sweep under *Commands*. Those are literals, not damage, which is why `scripts/check-encoding.mjs` skips this file and every other file must be 0. **That script owns the sweep's scope** — which directories, which individually-named root-level files, and why that root list is the weak spot a new file falls through. **The same trap catches a `.ps1` you write to do the editing, and `WriteAllText` does not save you from it:** PowerShell 5.1 decodes a BOM-less script file as ANSI as well, so non-ASCII prose sitting in a string literal *inside* the script is already mojibake before the correct writer emits it. Observed on 2026-08-13 while splitting this file — the moved text came through byte-perfect and every em dash in the new prose the script carried was corrupted. Keep generator scripts pure ASCII and have them read any prose they emit from a UTF-8 file written with the Write tool.
- **The working tree is a git repository**, initialised on 2026-08-09. `git diff` and `git log` work; there is still no remote, so nothing has been pushed. Read `git log` rather than trusting a commit count written here.
- **Two email addresses, deliberately not the same one. Do not reconcile them.** Commits are authored as `waqarshaikh2002@gmail.com` (set in the *global* git config, with no repo-local override, so one place controls it). The project's public contact address is `waqarsayyed.official@gmail.com`, and it is the one in `SECURITY.md`, `CODE_OF_CONDUCT.md` and `ContactPage.tsx`'s split `MAIL_USER`. They are a git account and a support inbox, owned by the same person for different purposes. The mismatch looks like an oversight and is not: the owner asked for them kept apart on 2026-08-29. **The git address must never be used as a contact address** — it belongs in commit metadata and nowhere else, so it must not reach `SECURITY.md`, `CODE_OF_CONDUCT.md`, `ContactPage.tsx`, the issue templates or `README.md`. Check with `git grep -l waqarshaikh2002`, which should name **this file and nothing else**: the bullet you are reading has to quote the address to state the rule, exactly as `scripts/check-encoding.mjs` has to skip `CLAUDE.md` for containing the mojibake pattern it hunts. An earlier version of this bullet said the address must appear in no tracked file at all, which it violated in the act of saying so. Note it is in the author metadata of **every** commit and therefore public in any clone regardless — that is the point of it being the *git* address, and it is not something to hide. (This sentence used to say "all 186 commits" and was 11 short by 2026-09-01. A running total is the one number here guaranteed to be wrong tomorrow, and the bullet two above already says to read `git log` rather than trust one written down; do not put a count back.)
- **Write commit messages to a file and use `git commit -F <file>`, never `-m` with a here-string.** The commits here have long explanatory bodies, and PowerShell 5.1 re-parses a string containing `"` before handing it to a native executable — so a `@'…'@` here-string carrying quoted text arrives at `git` split into dozens of arguments. Observed: one message produced ~45 `error: pathspec '…' did not match any file(s)` lines and committed nothing. Write the message with the Write tool (UTF-8, and the same mojibake rule as above applies to the message file), then `git commit -F`. If you must use `-m`, the message has to contain no double quotes at all.
- **`README.md` exists and is public-facing** — it is the open-source front door, not a duplicate of this file. It carries setup, a Firebase bring-your-own-project walkthrough, and the honest limitations list. It restates a handful of facts that also live here (the key story, the script list, the Node floor, the test timings), so a change to one of those is a change to two files. It deliberately carries no test counts — see the bullet above. **The one part of it that keeps going stale is the tree map** — the `## Project structure` block, the only copy of the file layout anywhere in the repo, so nothing cross-checks it. Its `lib/` line claims to list *everything the unit suite covers*, which makes every new tested module a change to that block; it has been caught up three times (`313cfdc`, `0e8e0b5`, and again on 2026-08-27, when `datetime`, `modelCatalog` and `reportGeometry` had all been missing). If you add a file under `src/lib` or `src/services`, that line is the second file to edit.
- **`CONTRIBUTING.md` is the public half of this file, and it is the copy sessions forget.** It opens by saying so — *"the other half is `CLAUDE.md` … where the two overlap, a change is a change to both"* — and this file did not say it back until 2026-09-01, so every "change to two files" warning above was counting `README.md` and stopping. It restates the Node floor, the six checks and the order CI runs them, the encoding rule, the `npm ci`-not-`install` reason, and the opt-in hook. **Three documents now carry the check list — this file's *Commands* block, `README.md`'s script table, and `CONTRIBUTING.md`'s table of six — so adding or renaming a script is a change to all three.** Note its table says *six* where `.github/workflows/checks.yml` runs *seven*, and that gap is deliberate and explained in both places: the seventh is `npm ci`, which nothing local can perform. The two files divide by audience, not by content: `CONTRIBUTING.md` tells a human what to run and what a PR must contain, this file tells a session why each constraint exists and what broke to produce it. Put process in `CONTRIBUTING.md`, reasoning in the note that owns the area, and only session-wide constraints here.
- **The licence is Apache-2.0, declared on 2026-08-29, and it is stated in four places that must agree.** `LICENSE` (the full text, © 2026 Waqar Sayyed), `"license": "Apache-2.0"` in `package.json`, the *License* section of `README.md`, and the SPDX header at the top of `src/App.tsx`. Changing the licence is a change to all four. Until that date there was none, and the SPDX header in `App.tsx` — a leftover from the AI Studio scaffold, like `"name": "react-example"` was — sat there contradicting a `README` that said no licence had been declared; Apache-2.0 was chosen partly because it makes that header correct rather than requiring its removal. **Do not add per-file licence headers to anything else.** One header, one `LICENSE`, one field: a header on every file is noise that drifts, and the SPDX one only survives because it predates the decision and now matches it.
- **The app owns no Gemini API key.** Each user supplies their own at runtime and the browser calls Google directly with it. This is the correction to a real leak incident — see *The Gemini call runs in the browser* in [`docs/notes/gemini.md`](docs/notes/gemini.md).

Stack: React 19, Vite 6, Tailwind v4, TypeScript, Express (dev *and* prod server), Firebase Auth + Firestore, Gemini via `@google/genai`. Entry points are `server.ts`, `src/main.tsx` → `src/App.tsx`, and `src/services/geminiService.ts`.

Most of the reasoning behind this codebase is **not in this file**. It lives in five notes under `docs/notes/`, split out because carrying all of it in every session cost roughly 24k tokens to answer a question about one area. (Four of them on 2026-08-13; `testing.md` followed on 2026-09-06, when the test inventory alone had reached 12% of this file.) **They are not loaded automatically, and the one-line summaries below are not a substitute for them.** Open the note that owns what you are touching before you change it: each records the incidents that produced the current shape, and the cost of skipping it is re-breaking something that was already fixed once.

| If you are touching… | Read |
|---|---|
| the API key, `.env`, `envPrefix` | [`gemini.md`](docs/notes/gemini.md) — *The Gemini call runs in the browser* + *Where the user's key lives*. Note this row is about the key, **not** about the server: for what `server.ts` actually does, see the two rows at the bottom of this table |
| a model id, a 404/429/503, `thinkingBudget` | [`gemini.md`](docs/notes/gemini.md) — *The model is auto-detected* + *Chat and report generation are separate paths* |
| the prompt, `layout` schema, mockup rendering, image crops | [`gemini.md`](docs/notes/gemini.md) — *`geminiService.ts` is a single mega-prompt* |
| **any REPX syntax question at all**, `Ref`, data binding, `ExpressionBindings`, `src/lib/repxRefs.ts`, `repxBindings.ts`, `repxBindingPlan.ts` | [`gemini.md`](docs/notes/gemini.md) — *DevExpress can be asked directly, and it answers*. **Read this before writing any REPX syntax from the DevExpress API docs**: those describe objects, not the file, and a twenty-line console program against the installed 20.1 assemblies will tell you what the serializer actually writes. It is also where the `Ref` uniqueness defect is recorded — duplicates make the loader discard content with no error |
| a unit conversion, `ReportUnit`, a font size in the REPX, `src/lib/reportGeometry.ts` | [`gemini.md`](docs/notes/gemini.md) — *The units audit (2026-08-26)*. Four coordinate systems meet in the pipeline and `reportGeometry.ts` is the only place a conversion may be written; every mismatch renders a plausible layout in the wrong place and never throws |
| a page margin, whitespace the model drew instead of declared, `src/lib/repxMargins.ts` | [`gemini.md`](docs/notes/gemini.md) — *The margin is drawn, not declared*. The first generated report opened in a real 20.1 designer matched its source and still declared `Margins="0, 0, 0, 0"`: the model reproduced the margin faithfully by pushing all 31 controls inward rather than by declaring it, which is a report DevExpress cannot re-margin |
| a truncated or half-written REPX, `src/lib/repxTruncation.ts` | [`gemini.md`](docs/notes/gemini.md) — *The report that finished without the REPX finishing*. Asked at the parse rather than at Export, and deliberately not `checkRepx`: that one answers *will the designer open this*, which a hand-written malformation fails the same way, and truncation is a specific enough diagnosis to repair automatically |
| a repeated `Font`/`ForeColor`, a `<StyleSheet>`, `src/lib/repxStyles.ts` | [`gemini.md`](docs/notes/gemini.md) — *The style sheet, and one attribute that changes its name*. Every control the model writes carries its own appearance, so one heading treatment used forty times is forty edits that have to agree; note a control's `Borders` is a style's `Sides`, which is the kind of thing only a measurement finds |
| file upload, PDF handling, `.repx` intake | [`app-shell.md`](docs/notes/app-shell.md) — File intake |
| which pane the bench shows, adding a sixth plate, `src/lib/workspaceView.ts` | [`app-shell.md`](docs/notes/app-shell.md) — *The result views are derived, not stored*. Two functions derive in opposite directions and must agree; a plate in one and not the other highlights its button and never opens its pane |
| the Preview pane, pagination, PDF export, `src/lib/reportPreview.ts` | [`app-shell.md`](docs/notes/app-shell.md) — *Preview is the only pane drawn from `repxContent`*. It is the only thing in the product that shows the artifact actually downloaded, which is why a page-height Detail band was invisible before it existed |
| dragging or retyping a control, `src/lib/repxEdit.ts` | [`app-shell.md`](docs/notes/app-shell.md) — *Editing lives on Preview, not on Mockup*, and the two render defects that no check here would catch |
| the Data tab, a pasted schema, `src/lib/dataSource.ts` | [`app-shell.md`](docs/notes/app-shell.md) — *The Data tab's three states* (the pane), then [`gemini.md`](docs/notes/gemini.md) — *What binding does* (the REPX it writes). Both, in that order: the ordering bug was in the pane and the syntax is measured in the note |
| a parameter, `FilterString`, `ObjectStorage`, `src/lib/repxParameters.ts` | [`gemini.md`](docs/notes/gemini.md) — *Parameters, and the first measurement that was a negative result*. Inline `Type=` is accepted and silently ignored, so the conversion has to be code rather than a prompt instruction |
| a group band, `GroupFields`, a subtotal, `src/lib/reportBands.ts` | [`gemini.md`](docs/notes/gemini.md) — *Grouping, measured the same way* + *The report is banded now* |
| a chart, a cross-tab, `XRChart`, `XRCrossTab` | [`gemini.md`](docs/notes/gemini.md) — *Charts and cross-tabs, and the pattern that finally became a rule*. That pattern — an item inside a collection carries no `ControlType` — is now four measurements deep and applies well beyond charts |
| the REPX audit, a finding `code`, the status-bar error/warning count, `src/lib/repxAudit.ts` | **That file's own doc comment for why a check exists**, [`gemini.md`](docs/notes/gemini.md) for how each was measured against DevExpress, and [`app-shell.md`](docs/notes/app-shell.md) — *The REPX audit is a count in the status bar* — for where it runs and what the user sees. It asks what is still wrong *after* the repairs, so an error means a repair declined; it gates nothing, and that is deliberate |
| the batch queue, the `.zip`, `src/lib/batchQueue.ts`, `src/lib/zip.ts` | [`app-shell.md`](docs/notes/app-shell.md) — *Batch is a rail panel*. One file at a time is a quota decision, not a performance oversight |
| the Revisions panel, `src/lib/revisions.ts` | [`app-shell.md`](docs/notes/app-shell.md) — *Revisions is the fifth rail panel* for the panel and why a drag is not a revision; [`persistence.md`](docs/notes/persistence.md) for storage, and read that one before touching deletion |
| the workspace shell, any `wb-` class, `src/workspace.css` | [`app-shell.md`](docs/notes/app-shell.md) (the 2026-08-12 port) + [`styling.md`](docs/notes/styling.md) (the artifact token aliases) |
| dragging or collapsing the review column, `src/lib/panelSize.ts` | [`app-shell.md`](docs/notes/app-shell.md) — *The review column is resizable and collapsible*, the first deliberate departure from the port |
| a route, a deep link, `document.title` | [`app-shell.md`](docs/notes/app-shell.md) — *Routing is real paths* |
| the progress bar, pause/stop, streaming | [`app-shell.md`](docs/notes/app-shell.md) (Progress) + [`gemini.md`](docs/notes/gemini.md) (*Chat and report generation are separate paths*) |
| saving/loading reports, sign-in, `firestore.rules` | [`persistence.md`](docs/notes/persistence.md) |
| `firebase-applet-config.json`, `databaseId`, the `AIzaSy…` string in the repo root | [`persistence.md`](docs/notes/persistence.md) — its `firestoreDatabaseId` must match `databaseId` in `firebase.json` or you silently talk to the wrong database. That key is a **public project identifier and stays committed** — it is not the class of value a Gemini key is; `README.md` says so too |
| the account menu, `AccountDialog.tsx`, `UserAvatar.tsx` | [`app-shell.md`](docs/notes/app-shell.md) — *Account management lives in `AccountDialog.tsx`* + [`persistence.md`](docs/notes/persistence.md) for what deleting an account has to remove |
| a marketing page (`LandingPage`, `LoginPage`, `DocsPage`, `FeaturesPage`, `ContactPage`), `SiteHeader`/`SiteFooter`/`MobileNav`, anything under `src/components/landing/` | [`app-shell.md`](docs/notes/app-shell.md) (the pages and their shared chrome) + [`styling.md`](docs/notes/styling.md) (`Logo`, the `.landing` scope) |
| `/terms`, `/privacy`, `LegalPage.tsx` | [`app-shell.md`](docs/notes/app-shell.md) — the sign-up form links them, so they are not optional pages |
| the home page announcement dock, `src/lib/announcements.ts` | [`app-shell.md`](docs/notes/app-shell.md) — the 2026-08-14 dock |
| theme, tokens, animation, the logo, `src/index.css` | [`styling.md`](docs/notes/styling.md) — and the landing palette/typography mirrors the YAML frontmatter of [`docs/design/DESIGN.md`](docs/design/DESIGN.md) **by hand**, so changing one is a change to both. The **typefaces** are not in that note; see the `src/fonts/` row below |
| a font face, a `@font-face` block, the **typefaces** comment in `src/index.css` | [`src/fonts/README.md`](src/fonts/README.md) — the three families have been **self-hosted** since 2026-08-30 (`506da99`, audit PERF-002), as variable `woff2` with their SIL OFL texts beside them. Two consequences: the licences are committed and per-family copyright is recorded, which is the standard `.agents/` failed; and `fonts.googleapis.com`/`fonts.gstatic.com` could leave the CSP, so adding a webfont from a URL puts an origin back into `style-src`/`font-src` and is a security change, not a styling one |
| the OG card, the favicon, `assets/source/`, anything in `public/` | [`app-shell.md`](docs/notes/app-shell.md) — `public/og-card.png` is **pre-rendered** from `assets/source/og-card.html` by a headless-Edge command recorded there, not by a build step, so editing the HTML changes nothing until you re-run it. The masters live outside `public/` because `public/` is copied verbatim into `dist/`; [`styling.md`](docs/notes/styling.md) owns which logo variant renders when. The row above sends you to the wrong note for this one |
| the **Open in designer** button, `src/lib/designerBridge.ts`, anything under `tools/` | [`tools/RepxDesigner/README.md`](tools/RepxDesigner/README.md) — one of **two** docs that live outside `docs/notes/`, so a grep of the notes finds neither. The other is [`tools/RepxProbe/README.md`](tools/RepxProbe/README.md), the console tool that measures what the DevExpress serializer actually writes; unlike the designer it opens no window, so it is safe to run from an agent shell |
| a test, a test count, what the suites do and do not cover | [`testing.md`](docs/notes/testing.md) — the per-file inventory, the counting traps, the three rules files, and what `test:coverage` measures. **It is the only place the numbers are written down**, so adding a test is a change to it; recount by running the suite, never by grepping |
| the CSP, any response header, `connect-src`, framing, HSTS | [`src/server/securityHeaders.ts`](src/server/securityHeaders.ts) — **the reasoning is that file's own doc comment, and there is no note.** `connect-src` is the directive the whole thing exists for: it is what makes a stolen key worthless to injected script, so read any addition to that list as a change to the app's central security claim. `script-src` carries no `'unsafe-inline'` in production — `index.html`'s one inline script is allowed by SHA-256 hash, which `securityHeaders.test.ts` recomputes from `index.html`, so editing that script without it fails the suite instead of silently restoring the flash of light theme. The dev policy is looser on purpose; `e60c285` is where a policy that ignored that broke every hot reload |
| `Cache-Control`, compression, what `npm start` serves, the port or bind address | [`src/server/staticCache.ts`](src/server/staticCache.ts) and [`bindHost.ts`](src/server/bindHost.ts) — doc comments again, no note. `index.html` must **not** get the immutable policy the hashed bundles get, or a deploy never reaches a returning visitor; `server.ts` mounts `compression()` in production only. `resolveBindHost` binds loopback in development because the dev server runs Vite in middleware mode, and binding every interface would publish the module graph to anyone who can reach the machine |

If what you are changing is not in that table, grep the notes before concluding it is undocumented — they are prose, and this index is deliberately shorter than they are. Grep `tools/RepxDesigner/README.md` too; it is the fifth note in everything but location. **And grep the source**: the last three rows point at doc comments rather than notes, because `src/server/` and `src/fonts/` document themselves at the point of use. A grep of `docs/` finds none of that — `git grep -n "Content-Security-Policy"` does.

**When you add to the record, it goes in the note, not here.** A new incident, a new invariant, a new reason something is the way it is — all of that is area-specific and belongs in the note that owns the area. This file grows only for what every session needs regardless of what it is touching: the constraints in *Read this first*, the commands, the checks below. Appending area-specific reasoning to the index is precisely how it reached 94,616 characters before the split on 2026-08-13, and it is the one habit that will undo that.

## Project

**Forma** — an AI-powered DevExpress report designer. The user uploads an image, PDF, or `.repx` file; Gemini analyzes it and returns three artifacts in one structured JSON response: a markdown spec, a `layout` JSON used to draw an in-browser mockup, and `repxContent` — valid DevExpress `XtraReportsLayoutSerializer` XML that downloads as a `.repx` file.

`docs/PRD.md` is the product spec and **the single copy**, kept current against the code — including its "not yet exposed in the UI" note (RTL, stored-procedure name, data-binding schema), its §5 record of the resolved key exposures, and its §6 statement that **moving generation server-side is explicitly out of scope**, not future work. If you change any of those areas, update it in the same pass.

**There is no quarantine folder, and its absence is a decision rather than an oversight.** A `_not_required/` directory existed from 2026-08-28 to 2026-08-29, holding parked files with a tracked manifest, its own `.gitignore` rules, a CI check and a pre-commit rule. It was retired: **git history is the recovery mechanism this project actually uses**, and a second one requiring three separate enforcement mechanisms was not earning its keep.

**So removals go through `git rm`, and recovery goes through `git show <commit>~1:<path>`.** Every removal site in the source carries a comment naming its commit — see `src/lib/motion.ts`, `src/components/landing/icons.tsx` and `sections.tsx`, and the `.bento-grid` note in `src/index.css`. The four files parked before the folder existed are at `9e92b86~1`, among them `metadata.json` — the AI Studio applet metadata, and the last place the pre-Forma name "DevExpress Report AI Designer" survived.

The practical consequence for a session: **if you are looking for something that used to exist and a grep finds nothing, search history before concluding it was never here.** `git log --diff-filter=D --name-only` lists every path ever deleted, and `git log -S'<symbol>'` finds the commit that removed a given piece of code.

## Commands

```powershell
npm run dev          # tsx server.ts -> Express + Vite middleware on http://localhost:3000
npm run dev:log      # same server, output also mirrored to dev-server.log (gitignored)
npm run build        # vite build (client) + esbuild (server) -> dist/
npm run build:server # server bundle only -> dist/server.cjs
npm start            # node dist/server.cjs -- serves dist/ statically, no Vite
npm run lint         # tsc --noEmit  (there is still no ESLint config)
npm run lint:encoding # node scripts/check-encoding.mjs -- mojibake AND literal control characters, as a gate
npm run lint:links   # node scripts/check-links.mjs -- relative links and cited section headings that no longer resolve
npm run check:size   # node scripts/check-bundle-size.mjs -- artifact budgets; needs a build first
npm test             # vitest run -- the pure helpers in src/lib + geminiService
npm run test:watch   # the same suite in watch mode
npm run test:coverage # the same suite + v8 coverage over lib/ and services/ only
npm run test:rules   # security-rule tests against the Firestore emulator (needs Java)
npm run clean        # removes dist/ (node fs.rmSync, works on Windows and POSIX)
npm run preview      # vite preview against dist/
```

**These seven commands are the whole of what a machine checks here.** Nothing else in this document is enforced — every other invariant it records is verified by reading or by looking at the rendered page:

```powershell
npm run lint                                            # tsc --noEmit; the baseline is clean, so any output is yours
npx tsc --noEmit --noUnusedLocals --noUnusedParameters  # dead code; also clean, so treat any hit as newly introduced
npm run lint:encoding                                   # the mojibake sweep; exits 1 with the offending paths
npm run lint:links                                      # every relative link and cited heading in the docs still resolves
npm test                                                # the pure helpers
npm run test:rules                                      # firestore.rules, via the emulator (needs Java)
npm run build; npm run check:size                       # artifact budgets; must follow a build
```

**`check:size` was added 2026-08-29, because a real defect got past every other check on this list.** `src/index.css` had a bare `@import "tailwindcss"`, so Tailwind's automatic content detection scanned all 110 markdown files and generated real utilities from class names quoted in prose — `.bg-user-msg` was being served to every visitor because a cleanup report contained the sentence explaining that the token was dead. `tsc` was clean, the suite passed, the build succeeded, and the stylesheet quietly grew. Nothing was watching the size. The fix is the `source(none)` block at the top of `src/index.css`; the budget is what stops it recurring. **The rest of the reasoning — how tight a budget has to be to trip, and the difference between legitimately raising one and retuning one that has crept to 99% — is the doc comment at the top of `scripts/check-bundle-size.mjs`, which is longer and more current than any summary kept here.** Full write-up in commit `0bf3291`. (A `lint:quarantine` check was added beside it and retired the same day with the quarantine folder it policed.)

**`lint:links` was added 2026-09-06, and it checks a promise this file makes about itself.** The documentation here is a router: this file is short because the reasoning lives in `docs/notes/` and in doc comments, and the *Architecture* section promises that a heading quoted here is findable with `grep -rn "<name>" docs/notes/`. Nothing checked that, and a rotted pointer is worse than a stale sentence — a session follows it, finds nothing, and concludes nothing was written. Checking all 22 citations by hand found one broken: a row cited a heading ending "the first measurement that was a negative result", while the heading itself wrapped the word *negative* in asterisks — emphasis inside a heading, so the literal grep missed it. **The comparison is deliberately strict — case and whitespace only.** Stripping markup from both sides makes the check pass on exactly that defect, which an earlier draft of the script did; its doc comment records why, and why the citing file is searched for headings only. Keep headings plain if you want them found.

**`.github/workflows/checks.yml` runs all seven, plus one thing the list above does not have** — the first five as one job that ends with `npm run build` (which catches a broken import or an unresolvable local module in the server bundle, and no test does), and the rules suite as its own job because it needs a JVM. Two details worth knowing before you treat the workflow and this list as the same thing: the unit step is **`npm run test:coverage`, not `npm test`** — same suite and same pass/fail, but the summary lands in the log so a drop shows up in a run's diff — and `npm ci` is used rather than `npm install`, which fails when `package.json` and `package-lock.json` disagree and is therefore an eighth check nothing else performs. Added 2026-08-27, and note it has never executed: there is still no remote. Until there is, these are run by hand exactly as before, and the workflow is a statement of intent rather than a gate.

**`npm run test:coverage` prints a deliberately scoped number.** `vitest.config.ts` includes only `src/lib/**` and `src/services/**`, so `App.tsx` and the twenty components are not in the denominator — do not "fix" that by widening the include list; raise the number by shrinking `App.tsx`. And the two 0% entries in the report are not untested paths. Both points are explained in [`docs/notes/testing.md`](docs/notes/testing.md), and the config says the same thing at the point of use.

**The encoding check was the one with no runnable form outside PowerShell**, which is why it got skipped and why it now has `scripts/check-encoding.mjs`. It sweeps `src/`, `docs/`, `tests/`, `scripts/`, `tools/`, `.github/` and `.claude/`, plus the root-level files named in its own `ROOT_FILES` — and **that list is the sweep's weak spot: a new root-level file is not checked until someone adds it there.** It has caught that twice already, on `CONTRIBUTING.md` and on `package.json`. `CLAUDE.md` is skipped, because **this file contains the pattern as prose and scores 8**, so its hits are literals rather than damage. **It also flags literal control characters as of 2026-09-05**, which is a second kind of invisible damage and needed its own check: mojibake at least renders as *something*, while a raw NUL or DEL renders as nothing, survives no copy-paste, and can silently change what a regular expression matches. **Every one of those decisions is documented at the point of use, in that script's own doc comments, which are the current copy** — including why its source is pure ASCII down to the pattern, why `.json` is an accepted extension, why `settings.local.json` is exempt, and why it fails when it sweeps implausibly few files. Read them before editing it.

The PowerShell one-liner below is kept for checking a single file by hand, or on a machine without the repo installed. **Do not keep a file count here** — an earlier version said 75 when the answer was 74, and the number's only real job is catching a sweep that silently stopped matching files, which `scripts/check-encoding.mjs` now does itself by failing if it swept implausibly few. `bin`/`obj` are excluded because the DevExpress build output under `tools/` is not source:

```powershell
$f = @(Get-ChildItem src,docs,tests,scripts,tools -Recurse -File -Include *.ts,*.tsx,*.md,*.css,*.mjs,*.cs,*.html | Where-Object { $_.FullName -notmatch '\\(bin|obj)\\' })
$f += Get-Item *.ts,*.md,index.html,firestore.rules,.gitignore,.env.example
$f | Sort-Object FullName -Unique | Where-Object Name -ne 'CLAUDE.md' | ForEach-Object { $n = [regex]::Matches([IO.File]::ReadAllText($_.FullName),'Ã|Â|â€').Count; if ($n) { "$n  $($_.FullName)" } }
```

**There is also a pre-commit hook, and it is opt-in.** `.githooks/pre-commit` blocks files over 1 MB, backup and archive extensions, OS droppings, and what looks like a real API key *assignment* (the `AIzaSy` prefix alone would fire on three legitimate places, so it is not used as a signal on its own). Git does not run hooks out of a tracked directory, so enable it once per clone with `git config core.hooksPath .githooks`; bypass a single commit with `--no-verify`. **The logic is in `.githooks/pre-commit.mjs`, in Node, and the shim explains why:** the first version was a shell script and could not run here at all — git on this machine is **MinGit**, which ships `usr/bin/sh.exe` but none of the MSYS userland, so `wc`, `tr` and `grep` are all missing. A hook that cannot execute is worse than no hook, because it looks installed. If you rewrite it, verify it by making a commit that should fail.

Each of the checks is explained where it belongs — the two suites in [`docs/notes/testing.md`](docs/notes/testing.md), the `strict` note and the mojibake incident in *Read this first*, the unused sweep under *`src/App.tsx` is the whole app* in [`docs/notes/app-shell.md`](docs/notes/app-shell.md), and the three check scripts in their own doc comments. This block is a runnable summary of those, deliberately carrying no test counts and no second copy of the reasoning.

- **Never run bare `vite dev`.** The dev server is `server.ts`; Vite runs in middleware mode inside it.
- The dev server belongs in the user's own terminal, not an agent background shell — it is long-lived and browser-facing. When you need to see its output, ask the user to run `npm run dev:log` and read `dev-server.log`; only take the port yourself for a short, self-contained check, and free it afterwards.
- **To actually run the app — launch it, attach a file, generate, screenshot — follow [`.claude/skills/run-forma/SKILL.md`](.claude/skills/run-forma/SKILL.md) rather than rediscovering it.** It carries the four-step sequence and a dependency-free CDP driver beside it, and exists because every session that wanted to see the UI rebuilt the same thing: `VITE_FORMA_MOCK` plus a seeded `sessionStorage` key unlocks the whole workspace offline, the Claude-in-Chrome extension cannot reach this machine's localhost so it has to be headless Edge, and both the file-attach and the type-into-React steps have a CDP path that silently does nothing. The `/run` skill finds it on its own; this line is for the sessions that do not invoke it. Keep `drive.mjs` pure ASCII, for the PowerShell reason in *Read this first* — `.claude/` has been inside the encoding sweep since 2026-09-05, so that is now enforced rather than requested.
- **`RepxDesigner.exe --serve` belongs in the user's terminal for the same reason, and a stronger one.** A process paints on the desktop of the session that created it, so an instance you start from an agent shell opens the designer where nobody can see it — and reports success all the way back to the browser. Ask the user to double-click it. See *Start it yourself, from your own desktop* in [`tools/RepxDesigner/README.md`](tools/RepxDesigner/README.md).
- **`npm run dev:log` is `node scripts/dev.mjs`, not `npm run dev` with a redirect.** It spawns the same `tsx server.ts` and tees the output, so the log exists only while that script is the thing running — a server started any other way writes no `dev-server.log`, however long you wait for one. It opens the file with `flags: 'w'`, so each run **truncates** the previous log and stamps a `=== npm run dev:log - <ISO> ===` header — check that timestamp before trusting what you read, and note ANSI codes are stripped on the way in, so the file is greppable while the terminal stays coloured.
- `scripts/build-server.mjs` bundles `server.ts` with esbuild, keeping `node_modules` external and baking in `NODE_ENV=production` — so the started server takes the static-file branch and the Vite import is dead code. `npm start` therefore requires `npm run build` first, and must run from the repo root (the static path is `process.cwd()/dist`).
- **`.env` does not contain a Gemini key and must not.** `.env.example` documents three optional variables — `VITE_FORMA_MOCK`, `HOST` and `HTTPS` — loaded by `server.ts` via `import "dotenv/config"`; the server has no key-bearing routes left, and `envPrefix: ['VITE_']` means a `GEMINI_API_KEY` set there reaches neither the server code nor the browser. `.env*` is gitignored except `.env.example`. **`APP_URL` is not one of the three**, whatever an older copy of this bullet or of the note said — it was removed on 2026-08-27 as read by nothing (audit INV-006), a copy survives inertly in this machine's untracked `.env`, and you should neither restore it to the example nor write code that reads it. The rest lives in [`gemini.md`](docs/notes/gemini.md).
- **Run one file with `npx vitest run src/lib/repx.test.ts`, one case with `-t "<name>"` — and expect no speed-up.** Warm, one file and all fifty-two cost about the same ~5s, so narrowing buys clarity rather than time, and a slow first run is a cold cache rather than a hang. **If every jsdom file dies at once with `[vitest-pool-runner]: Timeout waiting for worker to respond`, nothing is broken and re-running will not help** — jsdom's cold load exceeds vitest's hardcoded 60-second worker-start timeout, so each retry is killed before it can finish warming the cache. Warm it up outside vitest, in a process nothing is timing:

  ```powershell
  node -e "import('jsdom')"    # takes as long as it takes; then npm test
  ```

  The measurements behind that, the eight files that opt into jsdom, and how to read a run where only those eight fail are in [`docs/notes/testing.md`](docs/notes/testing.md).
- **The rules suite runs under its own config, and the emulator wrapper is what makes it work.** `npm run test:rules` is `firebase emulators:exec --only firestore "vitest run --config vitest.rules.config.ts"` — that config on its own has no Firestore to talk to, and `tests/` sits outside `src/` so `npm test` cannot pick it up by accident. Running a *single* rules case needs a space-free regex to survive PowerShell's re-parsing of the quoted inner command; both that and the three-config split are in [`docs/notes/testing.md`](docs/notes/testing.md).
- **`.claude/settings.json` carries the permission allowlist, and it is the *committed* one.** Seven entries covering every command in the two blocks above — `npm run *`, `npm test`, `npm ls *`, `npx tsc *`, `npx vitest *`, `New-Item *`, `Get-Process *` — so the documented checks run without a prompt for anyone who clones. Note `npm test` needs its own entry: it is not `npm run test`, so `npm run *` does not match it. **`settings.local.json` is gitignored, which is the tool's convention and was the wrong way round here until 2026-08-30** — the personal file was the tracked one, so a contributor's own permission edits would have collided with the owner's. Its twelve entries had also fossilised: five were one-off probes from finished debugging sessions, one of them allowing a read of `App.tsx` around four specific line numbers. Put project-wide permissions in `settings.json`; leave anything of your own in `settings.local.json` and it stays yours. `dev-server.log` is gitignored by the blanket `*.log`, not by name.
- **`.antigravity/` is editor state, not project data** — a single ~22 MB `.pbtxt`. Nothing reads it and nothing builds from it. It is explicitly gitignored, and that entry stays: it matched no pattern originally, so a first `git add .` would have put 22 MB into every clone forever, and the editor recreates the folder whenever it is next used. **Deleted from disk on 2026-08-29** at the owner's explicit request, which took the working tree from 178.45 MB to 156.37 MB. Note what that means if it reappears: the folder was never tracked, so there was no git copy and the deletion was permanent — it is the one irreversible removal in the whole 2026-08-28 cleanup, and the only one made outside the quarantine convention. If you see it again it is new state, not the old file. Don't read it, don't index it, and don't count it as source.
- **`.gitignore` carries credential patterns that are a backstop, not permission.** `key.txt`, `*.key`, `*apikey*`, `*api-key*` and `secrets.json` are ignored so a key pasted into the working tree by mistake cannot be committed by a careless `git add .` — the same accident as the `envPrefix` leak, with fewer steps. They do **not** make the repo a place to keep a key: it belongs in the app's Settings dialog, which puts it in `sessionStorage` and never on disk (see *Where the user's key lives* in [`docs/notes/gemini.md`](docs/notes/gemini.md)). Also note `*.log` is what hides `dev-server.log` and `firestore-debug.log`, and `.env*` is ignored **except** `.env.example`.
- **`.firebaserc` is deliberately *not* ignored, and that is the point.** It names the deploy target (`forma-201ba`) and holds no credentials. Ignoring it is exactly how a fresh clone or a CI run deploys to whichever project the CLI last happened to select — see [`docs/notes/persistence.md`](docs/notes/persistence.md) for the time that went wrong. `.firebase/` (deploy caches, emulator exports) **is** ignored.
- `vite.config.ts` gates HMR on `DISABLE_HMR !== 'true'` and carries an explicit *"Do not modify — file watching is disabled to prevent flickering during agent edits"* comment. Leave that block alone. The `watch: { ignored: ['**/tools/**'] }` line beside it is load-bearing too: chokidar throws `EBUSY` on MSBuild's locked `bin/`/`obj/` files and takes the whole dev server down with it.
- Symbol names in this document are stable; line numbers are not. Grep for the identifier rather than jumping to a remembered offset — `src/App.tsx` has already grown past every line number an earlier revision of this file recorded.

## Architecture

Five notes under `docs/notes/`, one per area — **plus two READMEs under `tools/`, listed last because they are the two an area-based search misses.** The section headings inside the notes are unchanged from when that text lived in this file, so a heading quoted in a commit message or anywhere here still finds its text with `grep -rn "<name>" docs/notes/`.

| Note | What it owns |
|---|---|
| [`gemini.md`](docs/notes/gemini.md) | The browser-side Gemini call and why it is not on the server; where the user's key lives and how the vault encrypts it; the mega-prompt and the `layout` schema; runtime model detection and the 404/429/503 paths; the chat-vs-generation split and the streaming reply. |
| [`app-shell.md`](docs/notes/app-shell.md) | `src/App.tsx` and everything it still holds: the ported workspace, the marketing pages and their shared chrome, real-path routing, file intake and PDF text extraction, the REPX viewer, streaming progress and cancellation. |
| [`persistence.md`](docs/notes/persistence.md) | Firestore versus `localStorage` and the shapes that diverge between them, the cloud save budget, `firestore.rules`, and the sign-in fallback that was removed for being an authentication bypass. |
| [`styling.md`](docs/notes/styling.md) | Design tokens and the four-part theme resolution, the `.sheet` / `.landing` scopes, the icon set, the `Logo` component, and the division of labour between CSS helpers and Framer Motion. |
| [`testing.md`](docs/notes/testing.md) | Both suites: the per-file inventory and its arithmetic, why a grep undercounts by 22, the three rules files and what each proves, what a component test is and is not for, the cold-cache cliff that reads as a broken install, and the deliberate scope of the coverage number. The only place the counts live. |
| [`tools/RepxDesigner/README.md`](tools/RepxDesigner/README.md) | The Windows companion that opens a generated `.repx` in the real DevExpress designer: the loopback `--serve` listener on `127.0.0.1:7317`, its two guards, the feature-detected **Open in designer** button behind `src/lib/designerBridge.ts`, DevExpress 20.1 version matching, and the MSBuild build. Outside `docs/notes/` because it documents a C# project, not the web app. |
| [`tools/RepxProbe/README.md`](tools/RepxProbe/README.md) | The console tool that asks DevExpress what a `.repx` really contains: `emit` prints what the serializer writes for a feature, `inspect` reports what the loader sees in a file we produced and exits non-zero when content was silently discarded. Outside `docs/notes/` for the same reason as the row above. **Read it before writing REPX syntax from the DevExpress API docs** — those describe the object model, not the file. |

## Structure conventions

These were already followed everywhere before they were written down on 2026-08-29, which
is exactly why they are here: an unwritten convention is one careless commit from being
broken, and nothing in the toolchain enforces any of them.

- **`src/lib` is browser code. `src/server` is not.** `server.ts` imports
  `securityHeaders`, `bindHost` and `staticCache` from `src/server/`; nothing else may.
  Before 2026-08-29 the first two sat in `src/lib`, the directory whose defining property
  is that everything in it is compiled by Vite and shipped to the browser — a component
  could have imported `resolveBindHost` and nothing would have objected. **They stay
  under `src/` rather than a root-level `server/` for one reason:** `vitest.config.ts`
  has `include: ['src/**/*.test.{ts,tsx}']`, so moving those tests out silently drops
  **forty-two** cases. Widen that include in the same commit if you ever do move them.
  *Test placement* in [`docs/notes/testing.md`](docs/notes/testing.md) owns the
  arithmetic — this bullet used to state it as a before-and-after pair of suite totals,
  which was stale within days of being written.
- **File naming: `PascalCase.tsx` for React components, `camelCase.ts` for everything
  else.** 100% consistent today — **25** PascalCase components and **107** `.ts` files
  (that is `git ls-files src`, tests included; **repo-wide the answer is 114**, because
  `server.ts` and the four root configs are also `.ts`. State the scope whenever you
  quote this number — an unqualified "67" is the next thing someone will "correct" to
  73.) The single exception is `src/vite-env.d.ts`, which is Vite's own required name.
  Both counts move together, and **`git log` is the record of when**: a changelog of
  every file added lived in this bullet until 2026-09-06, and it was wrong twice in its
  own text before anyone read it.
  **Three traps, each of which has been written in here as fact at least once.**
  **(1) Do not "correct" the 25 to 30 by counting `.tsx` files.** There are 30, and the
  five that are not components are deliberate: `main.tsx` is the entry point, `icons.tsx`
  and `sections.tsx` export collections rather than one component, and
  `DataBinding.test.tsx` and `BatchPanel.test.tsx` are colocated component tests, named
  after the component they test exactly as `repx.test.ts` is named after `repx.ts`. Do
  not count them as components and do not rename them.
  **(2)** `git ls-files 'src/**/*.tsx'` answers **28**, because that pathspec does not
  match files sitting directly in `src/` and so drops `App.tsx` and `main.tsx`.
  **(3)** Count with `git ls-files | Where-Object { $_ -like '*.tsx' }` and *read the
  names*, rather than trusting whichever of the three numbers you got.
- **Test placement is two conventions, and the split is load-bearing.** Colocated
  `*.test.ts` beside the module for the unit suite; `tests/` at the **root**, outside
  `src/`, for anything needing the Firestore emulator — which is what stops `npm test`
  trying to start a JVM. A module can legitimately have a file in both; see *Test
  placement* in [`docs/notes/testing.md`](docs/notes/testing.md).
- **No barrel files.** There is not one `index.ts` re-export in this repository, and the
  Phase 4 dead-code pass measured the benefit: seven unused exports were tree-shaken to
  exactly zero bytes. A barrel would have retained them. Do not "tidy" imports into one.
- **Where new code goes.** A pure helper → `src/lib`. A server helper → `src/server`. A
  component → `src/components`, or `src/components/landing/` if it is a landing-page
  figure. A new **top-level directory needs a stated reason** — and the point is to hold
  the line the 2026-08-29 cleanup drew. **There are ten tracked top-level directories:**
  `.claude`, `.githooks`, `.github`, `assets`, `docs`, `public`, `scripts`, `src`,
  `tests`, `tools`. Check with `git ls-tree --name-only -d HEAD`, which is the whole
  verification. It was thirteen before the cleanup: `.vscode` stopped being tracked
  (`5b90af9`), root-level `audit/` moved under `docs/` (`08561cd`), `_not_required/`
  was retired with the quarantine (`40bb18b`), `.agents/` went untracked on 2026-08-30
  (`ff3ac0e`), and `.githooks/` was added during the pass. (This bullet previously said
  "from ten to eight", which matched no commit — there were twelve tracked directories
  on the day it was written. Hence the list: a number nothing can check is how it went
  wrong, and `git ls-tree` settles the replacement in one command.)
- **`docs/` holds all the prose, in three kinds — plus `media/`, which is not prose.**
  `PRD.md` and `notes/` are *current* and maintained against the code.
  `design/DESIGN.md` is the design source of truth.
  **`docs/media/`** (2026-09-01) holds images the committed documentation links to, and is
  the *only* place a screenshot may be committed — `CONTRIBUTING.md` bans them everywhere
  else, and that ban was absolute until the README needed one. Nothing in the build reads
  it; `public/` is Vite's static root and this is not it. It carries its own README with
  the capture rules, including deleting the placeholder in the same commit as the real
  capture.
  **`docs/audit/README.md`** is the 2026-08-27 findings register, reduced on 2026-08-29
  to a one-line index of the **31** identifiers the codebase actually cites. It exists
  because the ten narrative reports could be deleted and their identifiers could not:
  `ARC-001`, `PERF-002`, `REL-001` and the rest appear in comments across dozens of
  source and config files, and in the names of local branches, none of which an edit here
  can reach. **That file owns the counts and the regex that produces them** — a second,
  differently-measured copy of the citation count lived here until 2026-09-06, and of the
  two it was the one nobody re-ran. It is a **dated record, not live documentation**;
  read `notes/` for what is true now.
- **The 2026-08-28 cleanup pass lives in its commit messages, not in a document.** There
  were twelve phase reports under `docs/cleanup/`; they were **removed on 2026-08-29** at
  the owner's request and are recoverable from history (`git show 7b7de66:docs/cleanup/09-results.md`,
  or any commit before that one). The pass itself is 32 commits merged as `9a8163c`, and
  the messages carry the measurements — each one states what was changed, what it was
  worth, and what was verified. **`git log 2ce6e56..9a8163c` is the record**, and
  `git revert -m 1 9a8163c` still undoes the whole thing in one operation. Several
  decisions elsewhere in this file cite a specific commit for exactly this reason.
- **There is no `@` path alias, and every import in this project is relative — keep it
  that way**, or add aliases back to `tsconfig.json`, `vite.config.ts` and
  `vitest.config.ts` in one commit and actually use them. One was declared in all three,
  each resolving to the repo root with nothing checking they agreed, so changing one
  would have made the build and the tests resolve `@` to different places silently. **The
  prior question went unasked until the very end: nothing was using it** — not one import
  in `src/`, `tests/`, `server.ts` or `scripts/`. All three declarations were removed on
  2026-08-29 and the built output was byte-identical, which is the proof.
  `tsconfig.json` carries the full account where the `paths` block stood; commit
  `1e9b4e5` has the rest.

## Firebase skills

`.agents/skills/` holds Firebase's official skill packs — **untracked and gitignored as of 2026-08-30, still present on disk.** Four are kept, each mapping to something this app actually does: `firebase_basics`, `firebase_auth_basics` (sign-in), `firebase_firestore` (saved reports and the key vault) and `firebase_security_rules_auditor` (`firestore.rules`). **Consult the relevant `SKILL.md` before changing `firestore.rules` or auth flows.** If the folder is not there, that is expected rather than broken — install the official packs; nothing in the build or CI reads them. The `.gitignore` entry carries the reasoning for untracking them, which is licensing and provenance: they are Google's writing, they were 75% of every `.md` file this repo tracked, and they carry no `LICENSE`, no `NOTICE` and no statement of origin.

**Six packs were removed on 2026-08-29, and a pack refresh will offer to put them all back. Do not let it.** `firebase_ai_logic_basics`, `firebase_app_hosting_basics`, `firebase_crashlytics`, `firebase_data_connect_basics`, `firebase_remote_config_basics` and `firebase_hosting_basics` were 35 files of vendored documentation for products this app does not use — `CONTRIBUTING.md`'s rule covers it exactly: a file must be used by code that actually ships or actually runs. Hosting went last and **on evidence rather than assumption**, because "we might deploy to Firebase one day" is the argument that keeps this kind of file forever: `firebase.json` configures only Firestore and the emulators with no `hosting` block, there is no `apphosting.yaml`, production is served by the project's own Express bundle (`npm start` → `node dist/server.cjs`), and the single deploy instruction in `README.md` is `firebase deploy --only firestore:rules`. If Firebase Hosting is ever actually adopted, restore that pack then — `git log --diff-filter=D --name-only` finds it.

(`xcode_project_setup` shipped in the same folder and went earlier, with the quarantine four at `9e92b86~1` — this is a web project on Windows with no Apple target.)
