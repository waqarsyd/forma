# The app shell

Split out of `CLAUDE.md` on 2026-08-13, carried across unchanged at the time apart from heading levels and the cross-references, which name the note they point at instead of saying "above". **It has grown since, so do not read it as a snapshot of that date** — every note here has taken new material in the weeks after the split, and `git log -- docs/notes/` is the record of what arrived when. (This paragraph claimed the text was still unchanged until 2026-08-20, by which point all four notes had been edited four or more times.) It is incident history — the reason a thing is the way it is, which the code cannot tell you. **Nothing here is loaded automatically**: `CLAUDE.md` routes to this file, and reading its one-line summary of this area is not a substitute for opening it before you change that area.

## `src/App.tsx` is the whole app

It is several thousand lines (a figure that only ever grows — don't trust a number written here, and don't reintroduce one into this heading where it will silently rot). It holds the workspace, chat and config modal, plus two components still defined inline: `RepxViewer` (the XML pane, whose pure helpers were extracted to `src/lib/repx.ts` — see below) and `ReportMockup` (renders `layout` as absolutely-positioned divs).

**The chat's rules are in `src/lib/chatSession.ts` as of 2026-09-08, and the chat itself is staying where it is.** Five things were inline in `App.tsx` where no test could reach them: whether a turn is conversation or work, the history the model is actually sent, the two rewrites a retry performs, and the note count. They are pure functions now, with nineteen cases. Three details are worth keeping.

`ChatMessage` is **generic over both its result type and its attachment metadata** — `ChatMessage<DesignResult, PreviewMeta>`, aliased back to plain `ChatMessage` at the top of `App.tsx` so no use site changed. `DesignResult` lives in `App.tsx`, and a `src/lib` module importing from the component layer is the wrong direction; `savedReport.ts` had already solved this with `M` and `R` parameters it never looks inside. The metadata parameter is not decoration: the transcript hands `imageMeta` straight to `groupAttachments`, which groups by `uploadId` — exactly the field a "good enough" inline shape omits, so a structural minimum compiles in the module and fails at the call site. `ChatHistoryTurn` restates `ChatTurn` from `geminiService.ts` rather than importing it, for the same rule in the other direction: `lib` does not import `services`. The two are structurally identical, so the array is assignable with no cast and the compiler reports it at the call site if that ever stops being true.

**The history deliberately carries nothing but `role` and `text`, and that is the expensive one.** `images` holds base64 data URLs; sending whole message objects would re-upload every previously attached page on every subsequent chat turn, billed each time. It equally deliberately does **not** filter out a message carrying an `error`, or the assistant's own "Generation stopped by user." line — both are things that happened in the conversation, and hiding them produces a model answering as though they had not.

**The transcript render followed on the same day, as `src/components/ChatThread.tsx`, and the order it was done in is the point.** The markup had no coverage of any kind, so the component was written and its nineteen render cases were passing *before* `App.tsx` was switched over to it — extract-then-check would have been unverified by construction, which is the order that produced the bug `DataBinding.test.tsx` was added after. The test was then confirmed to have teeth by deleting the `!isAnalyzing` guard and watching the right case fail. Two checks agreed the swap changed nothing: the suite, and a real session whose screenshots came back **byte-identical** to the pre-extraction run at all three steps.

**The composer followed as `src/components/Composer.tsx`, in the same order and with the same result** — twenty-four cases passing before the swap, the screenshots byte-identical again, `App.tsx` down another 130 lines. Two of those cases exist for hazards the markup had only carried a comment about, and both were confirmed to fail when mutated. **The send button must be wrapped rather than handed its callback bare**: `onSend` is `handleGenerate`, whose first parameter is an optional prompt override, so `onClick={onSend}` passes React's synthetic click event as that override — truthy, so it generates against an event object instead of the typed prompt. The test asserts it is called with *no arguments*, which is what pins the wrapper. And **Enter sends while Shift+Enter must not**, or a deliberate line break fires the request. `fileInputRef` is passed in rather than owned, because `App.tsx` clears `.value` on it after an upload so the same file can be chosen twice running; owning it here would have broken that silently.

**Two real defects fell out of the extraction, and neither came from the suite.**

**Enter bypassed the send button entirely.** The button carried `disabled={!hasApiKey || isAnalyzing || isIngesting}`; the keydown handler on the input beside it carried nothing, so Enter sent regardless of what was already running — and `handleGenerate` had no re-entry guard, while `handleRetryMessage` had always had one. That asymmetry is what gave it away. Pressing Enter during a generation started a second one writing to the same progress state and the same `activePromptRef`; during a chat turn it started a second turn whose history came from a `messages` closure captured before the first reply landed, so the model was sent a conversation missing a turn. `isChatting` was not in the button's condition either, so that control was fully live throughout a chat turn. Fixed in both places — one `canSend` in the composer that the button *and* the keyboard read, and a guard in `handleGenerate` so it holds regardless of who calls. Five tests, written first and watched to fail.

**Then the fix broke focus, and only a pixel diff found it.** `.wb-box` draws its ring on `:focus-within`, so clicking send lights the composer because the *button* holds focus. The moment `canSend` included `isChatting`, that button began disabling itself mid-request — and a disabled element loses focus, which no browser gives back when it re-enables. Two screenshots that looked identical to the eye differed by 11,116 pixels, all inside the review column. **The same defect turned out to predate the change**: the button already disabled on `isAnalyzing`, so clicking send during a generation had always dropped the user to the top of the document. Sending now returns the caret to the field, which fixes both paths and is where the next keystroke wants to be anyway. Move focus *before* React disables the button, or there is nothing to move.

**The transcript is memoised, and the number is the argument.** Typing 22 characters into the composer with six notes on screen re-rendered every note on every keystroke — **264 note renders for one sentence**, each image-bearing note re-running `groupAttachments`, because the transcript is a child of the component that owns the composer's `prompt` state. Measured over CDP with a temporary counter, then again after: **264 → 0**. `ChatNote` is `memo`'d on its message, which is safe because messages are immutable. The hazard that introduces is a row keeping the *first* callback it was handed, whose closure holds the transcript as it was then — so the callbacks are read through a ref, and a test proves a retry reaches the newest handler. Confirmed by mutation: a comparator that ignores the callbacks fails exactly those cases.

**The designer probe is throttled on focus as of 2026-09-08, and the reason is the comment three lines above it.** Re-checking when the tab regains focus is deliberate — starting the companion happens outside the browser, so a mount-only ping leaves the button missing when the user switches back. But a refused connection is logged by the browser itself and no `catch` can suppress it, so someone alt-tabbing while they work produces one `ERR_CONNECTION_REFUSED` per switch. Four consecutive logs sent in from real use had those lines outnumbering everything else in the console, which is precisely the failure the effect's own comment already warned about: *a predictable error on every page load is noise that hides the unpredictable ones.* A focus re-check needs 30 seconds since the last one (`shouldRecheckDesigner`). Nobody launches a Windows desktop app and returns to the tab inside half a minute, so nothing that mattered was traded away.

**And the mount ping was narrowed the same day, by the same argument one step further.** The effect had already been scoped from "every page" to "the workspace"; but the workspace opens *empty*, and the button is disabled until a report has XML to open — so it was still probing before anything could use the answer. It now waits for `hasRepxToOpen`. Nothing degrades while unprobed: `designerReady` only picks the tooltip and decides whether a click sends to a running designer or launches one, and `false` takes the launch path, which is the correct fallback regardless. Measured over CDP with `performance.getEntriesByType('resource')` rather than the console — **a refused connection is logged by the browser's network stack, not through `console.*`, so it never appears in the driver's `console.txt` and cannot be counted there.** Empty workspace: 0 probes. After a generation: 1.

**The transcript follows the conversation as of 2026-09-08, and it had not since the port.** Reported from real use: once the transcript outgrew the column every reply landed below the fold and had to be scrolled to by hand. `App.tsx` had carried a `messagesEndRef` and a `scrollIntoView` effect since before the 2026-08-12 rebuild — but that rebuild replaced the markup with the artifact's, which has no sentinel div, so the ref attached to nothing and the effect ran against `null` on every message for weeks. **Neither the type checker nor the unused sweep can see this**: the ref and the effect reference each other, so both look live. `git log -S'ref={messagesEndRef}'` names `674f4cd` as the commit that took it, which is the port itself.

The follow now lives in `ChatThread`, which owns `.wb-thread` — the element with `overflow-y: auto` — and the dead ref and effect are gone from `App.tsx` rather than reattached. **The half that makes it bearable is `pinned`**: scrolling up to re-read an earlier answer must not be undone by the next reply arriving, so the position is recorded on every scroll and the follow only fires when the reader was already at the bottom. The 40px slack matters — a fractional `scrollTop` from a trackpad or a zoomed page otherwise reads as "not quite at the bottom" forever and the thread silently stops following. `scrollTop` is assigned rather than `scrollTo({ behavior: 'smooth' })`, because a streamed reply updates many times a second and successive smooth scrolls queue up and lag behind the text they are meant to follow. Verified live at 14 notes: content 1,319px in a 745px column, `distanceFromBottom` 0, newest note fully visible.

**Removing that markup orphaned four imports** — `IconArrowUp`, `IconDoc`, `IconPaperclip` and `groupLabel` — which `npx tsc --noEmit --noUnusedLocals` caught and nothing else would have. That is the same class of leftover as `MobileNav` when `LandingPage` came out, and it is the reason the unused sweep is on the check list rather than being folded into `npm run lint`.

`ChatThread` owns `.wb-thread` and takes the generation progress card as `children`. The card is generation state — the ring, pause and stop, the elapsed-time explanation — and pulling it in would have meant ten props about a job the transcript has no opinion on; passing it through keeps the emitted DOM identical to the markup it replaced, which is what made the byte-identical comparison possible. **The `!isAnalyzing` guard on the thinking placeholder is the subtle one**: a message the assistant judges to be a real build request runs the chat turn *and then* falls through to generation, so both flags are true together for a moment, and without it the placeholder sits above the progress card claiming a second job. `handleGenerate`'s branch and the composer are still in `App.tsx`.

**Do not turn the chat into its own page or route.** It reads like the obvious tidy-up — every other surface is a page — and it would break the product. The chat is the control surface for the report: a refinement turn sends the previous `layout` and `repxContent` so the model edits the existing report rather than starting again, and an attachment on a message *is* the request to build. A separate page is either severed from the report it operates on, or a duplicate of the whole result pipeline. Splitting it as **code** is worth doing; splitting it as a **surface** is not.

**All six page-level surfaces now live in `src/components/`**: `LandingPage`, `FeaturesPage`, `DocsPage`, `ContactPage`, `LoginPage` and `LegalPage` (added 2026-08-15, serving `/terms` and `/privacy` — see below), alongside `MobileNav`, `UserAvatar`, `AccountDialog` and `Logo`. `src/components/landing/` is **the marketing design system, not the home page's private folder** — the name is historical. Two tiers live there: `sections.tsx` (the reveal wrappers, `SectionHead`, `Eyebrow`, `Stat` and the `H2`/`LEDE`/`BODY` type scale), `icons.tsx` and `SheetRuler` are shared furniture — `LandingPage` and `FeaturesPage` both build on them and `SiteHeader` pulls `IconSun`/`IconMoon` from `icons.tsx`; while `HeroScanner` (the scan → detect → type sequence), `VaultFigure` and `StepFigures` really are home-page-only figures. Build a new marketing surface out of the first tier rather than restating its markup, and add new icons to `icons.tsx` rather than starting a second set. (This bullet used to read "landing-page-specific by design; nothing else imports them", which was already untrue of `icons.tsx` and stopped being true of the rest when the Features page was rebuilt on 2026-08-11.) The five pages that mount the chrome take the same props — `onEnterWorkspace`, `onSignIn`, `onSignUp`, `user`, `logOut`, `isDarkMode`, `setIsDarkMode` — and pass them straight through to it; `LegalPage` adds `doc` on top, to choose which of the two documents it renders. `LandingPage` was extracted on 2026-08-09; it had been inline while its three siblings were not, which is why header fixes kept having to be applied to it separately. Note that pulling it out orphaned `MobileNav`'s import in `App.tsx`, since the landing page was its only consumer there — the unused sweep catches that class of leftover.

**`DocsPage`, `ContactPage` and `LoginPage` were rebuilt on 2026-08-11, and each rewrite corrected something that had stopped being true.** The visual work is the smaller half; do not reintroduce any of these:

- **Docs** told readers Forma "operates using a shared developers API key" and that their own key was "stored securely within your browser's local state (`localStorage`)". The app owns no key, and that `localStorage` entry is *purged on boot* — describing plaintext on disk as secure was the worst line in the repo. It also put `overflow-hidden` on `<main>` while the sidebar used `sticky`; **an overflow ancestor silently disables sticky**, so the docs nav scrolled away. Nothing above that `<aside>` may set overflow.
- **Contact** displayed `support@forma.com` over a `mailto:` pointing somewhere else, linked Github/Discord/LinkedIn all at `/`, and — the serious one — its `catch` block set the success state, so a failed send told the sender "we've received your message" while dropping it. A rejected request must reach the failure state; there is no path from `catch` to `sent`. The address is now assembled from two halves at click time and never appears in the markup, and the form carries an off-canvas honeypot.
- **Login** kept its logic (that part was already careful) and lost its surface: a blurred ring pulsing behind the submit button on an endless loop, a "Verified Stack" badge asserting a guarantee nobody made, Privacy/Terms pointing at `/`, and two `rounded-xl` buttons — the only place in the app that broke the everything-is-a-pill rule.

**The home page has an announcement dock (2026-08-14)** — `src/components/landing/AnnouncementDock.tsx`, a 44px disc in the bottom-right corner that opens the product's notes, modelled on the corner launcher pattern Dribbble uses. Four things to know before touching it:

- **Home only.** It is a first-visit surface. `/features`, `/docs` and `/contact` are reference material people arrive at on purpose, and the workspace already carries a rail, a review column and a status bar — a disc floating over a tool someone is working in is one thing too many. Verified absent on all four — and the durable form of that check is that `AnnouncementDock` is imported by `LandingPage` alone, which is why `/terms` and `/privacy`, added later, inherited the rule without anyone applying it.
- **Bottom-right, and the right has nothing else in it.** It shipped bottom-left first and had to dodge `SheetRuler` (`fixed left-0 w-11` from `xl` up) with an `xl:left-16` offset — 44px of ruler plus the 20px inset. Moved to the right on request the same day, which is also the corner the reference uses; the offset went with it, and the panel anchors `right-0` because leftwards is the only direction with room.
- **Opening is what marks the notes read**, not a dismiss button: the dot exists to get the panel opened once, and a badge that survives being read is a badge people learn to ignore. The newest id goes to `localStorage['announcementsSeenId']`, and every `localStorage` access is guarded — a locked-down profile makes every visit a first visit, which is the right way to fail.
- **The entries live in `src/lib/announcements.ts`, newest first, and must describe the shipped app.** This is the one surface where the product talks about itself to someone who has not used it, so a note about something not actually there costs more than no note. `unseenCount()` is tested: an unrecognised stored id counts everything as unseen rather than nothing, because showing a stale dot is a smaller failure than never showing one again.

**`HeroScanner`'s page is drawn in report units, and that is load-bearing (2026-08-14).** `PAGE` is 850 × 1100 — the same units `LocationFloat` and `SizeF` carry — and five rectangles (`PICTURE`, `TITLE`, `BILL_TO`, `TABLE`, `TOTAL`) hold the controls in those units. The page is laid out from them and the typed XML is built from them; percentages exist only as the `px()` / `py()` output.

**The outlined "detected" regions and their coordinate captions are gone**, removed on request the same day. They covered the document they were pointing at, and once the page was properly typeset they were the only thing still making the figure read as a diagram rather than a scan. The sweep and the XML carry that story without annotating over the invoice. The rectangles stayed — they are geometry, not decoration.

It was three sets of numbers that disagreed, in a figure whose entire subject is coordinates being read correctly. The picture region was captioned `160,90` — 8.2% of the page — and drawn 14.2% tall; the table declared `SizeF="770,124"` (11.3%) and was drawn across 27.4%; the total was captioned `570,770` and drawn at 61.8%, 74% instead of 67.1%, 70%. **Every box asserted a number it did not honour.** Nobody would have caught it by looking, which is exactly why it is worth keeping the derivation: a caption cannot drift from its own geometry when it is generated from it.

**The second pass was about size and weight, and those numbers are worth keeping too.** The page was 212 × 274 inside a 273 × 474 pane — 200px of dotted background, 58% of the column's height — while the code pane beside it ran edge to edge; the document that is the subject of the figure was the smallest thing in it. Padding 22 → 16 and a 212 → 248 cap put the sheet at 240 × 311 and lifted the scale from 0.25 to 0.28 px per report unit, which is what makes the ruled table read as a table. **The rest of the slack was the code column, because that is what sets the card's height** — the page cannot grow past its own column's width, so every XML line beyond the sheet's height is dotted bed around it. Folding each element name onto its `<ItemN` line, which is how anyone actually reads REPX, took 23 lines to 18 without dropping an attribute; nothing may exceed ~42 characters there, because the pane wraps at that width and a wrapped line costs the same as a written one. Card 516 → 416, slack 82 → 32 a side, the sheet at 83% of its column. The `<pre>` also carries a `min-h` equal to the finished block, since the pane grows a line at a time while the XML types and the card — and the hero beside it — used to step taller eighteen times during the animation; measured after the change, the card holds 416 from first frame to last. The overlay was toned down in the same pass — fill from `--accent-wash` to `secondary-container/[0.07]`, captions from 12px to 5.5px — before being removed outright, which is the honest reading of that work: each step made the annotation quieter until the answer turned out to be *no annotation*. **The third pass threw the bars away, and that is the one that mattered.** The page had been grey rectangles standing in for words — "geometry rather than art", defensible in the abstract and wrong for this figure. A hero whose whole job is to say *this reads real documents and writes real files* cannot show a placeholder: bars read as unfinished, and unfinished reads as *does this actually work?* — which is exactly how it was reported. It is now typeset: an issuer block, invoice number and date, bill-to and ship-to, four line items with descriptions, quantities and amounts, subtotal, VAT, a filled total row, payment terms and a footer. The type is small because it is at **true scale** — 850 units shown 240px wide is 28px to the inch, so 10pt body copy really is ~4px, which is what a document thumbnail looks like and why it now reads as a page rather than a wireframe of one. Sizes are `cqw` against the sheet's own `container-type: inline-size`, so every glyph scales with the page instead of pinning itself to a pixel size the sheet may not have.

Three things clipped when the text went in, all fixed by giving glyphs the inset a real control has: amounts right-aligned to the 810 margin were cut by the table's own right border (they end at `CELL_RIGHT`, 790, now), and both the heading and every address line in the bill-to block lost its first character to the detected box's left border (inset 10 units). Text sitting exactly on `LocationFloat` renders *under* the box that marks it — a label's glyphs begin a few units inside its bounds, as they do in print.

**Replay was a dead control on any machine that prefers reduced motion, and Windows is one of them.** `run()` short-circuited to the finished state whenever `useReducedMotion()` was true — including a run the user had just asked for by clicking. Measured with the media feature emulated: after a click, the typed length stayed at its final 502 characters across eight samples, while the identical click under `no-preference` reset it to 0 and re-typed. **Chromium reports the preference whenever Windows' "Show animations in Windows" is off, which is the default on Server editions**, so this was not an edge case — those visitors never saw the animation at all and the button did nothing when they asked for it. `run(force)` now separates the two: arriving at the page still animates nothing under the preference, and a click animates regardless, which is the standing exception in WCAG's guidance for user-initiated motion. `animating` (per-run) replaced `prefersReduced` at all four use sites — the sweep, the box delays, the chip delays. Note the handler must be `() => run(true)`; passing `run` directly would hand it the click event as `force`, which is truthy by accident rather than intent.

The ink was redrawn as an invoice at the same time — masthead, bill-to/ship-to, a ruled four-row table on three column rails, payment terms opposite the totals, an emphasised total row, footer. Ink sits *inside* its region rather than filling it, because a region is the control's bounds and the ink is the glyphs; a 320-wide `XRLabel` holding the word INVOICE does not paint 320 units of black. The filled total row is the one deliberate exception. Verified over CDP in both themes: every box now lands within a unit or two of its caption (the residual is the sheet's 1px border at a 212px render), and the sheet stays white in dark mode, as `--paper` requires.

**Contact's headline is a full-width hero as of 2026-08-14, and the width is the reason.** It carries `FeaturesPage`'s type exactly — `text-display-lg md:text-[70px]`, `leading-[1.0]`, `tracking-[-0.042em]` — plus the accented trailing clause with the underline bar that Landing and Features both draw. It could not stay inside the two-column grid to do it: at 70px "Something not working," measures 689px, while the column beside a 560px form is 492px from 1280px up, because the container maxes out and never grows past that. Matching the other two pages' size and keeping that clause on one line are mutually exclusive in 492px and both hold across the container — on one line at full size with no `nowrap` holding it, so the markup is `inline-block` and nothing else, identical to the other two. The grid below is untouched; the "Before you write" card lost its `mt-7` because it is that column's first child now and has to start level with the form. Measured after the move: one line at 1024 through 1920, and at 760 where the base 48px applies.

**Docs also carried a reading-progress bar, and it is gone (2026-08-14).** A 2px `bg-secondary` strip pinned across the top of the viewport, scaled by `scrollY`, with the `progress` state and scroll listener behind it. No other page had one, which is the whole reason it looked wrong: the marketing pages share their chrome deliberately, so a fifth page growing an extra fixed element above the header reads as a rendering fault rather than a feature. Removed with its state and its listener, not just hidden. `SheetRuler` already reports position on this page and keeps its own listener — that one stays.

**`/terms` and `/privacy` are real pages as of 2026-08-15, and they had to be.** The sign-up form said *"By creating an account you agree to the Terms and the Privacy policy"* over two `href="#"` placeholders, and `SiteFooter` pointed its pair at `/` — a link that silently goes home rather than admitting it has nowhere to go, which is the failure `data-needs-url` exists to catch and which that pair slipped past. Both are now `LegalPage.tsx`, one component rendering either document because they are the same page twice. `SiteHeader`'s `NavItem` gained `'none'` for pages outside the nav; passing one of the four would light a link the reader is not on. **Everything in those documents is checkable against the code** — what is stored, where it goes, what is not collected — so when the app changes they change in the same commit. A privacy policy that has drifted from the software is worse than none, because it is a promise nobody is keeping.

**Account management lives in `AccountDialog.tsx`, reachable only from the workspace rail's account menu.** Rename, change password, change email, delete account. It is not in the marketing pages' profile menu: reaching it from there would mean threading a dialog through six pages for a screen nobody visits mid-scroll. It borrows the config dialog's keyboard contract wholesale — focus in on open, Escape closes, Tab cannot walk out — and the deletion is typed out in full rather than confirmed with one click, because it discards the user's saved work. The Firebase side of it is in [`persistence.md`](persistence.md).

**It had no render test until 2026-09-08, and it was holding four defects that all look fine in a screenshot.** None of them throws; that is why they survived. Written test-first: 21 cases, of which 10 failed before the fix.

**One `currentPassword` state sat behind all three sensitive actions, with inputs in only two of them.** Every consequence of that was wrong, and they compound. The *email change* required a password and had no field, so the one it read lived in the Password fieldset in the other column — its button simply stayed disabled with nothing on screen saying which box would enable it. Typing a password to *change* it also filled the *delete* confirmation, because both inputs were the same state. And satisfying one section silently satisfied half the guard on another, on the screen where that matters most. Each section now has its own field, beside the button it unlocks.

**Three smaller ones from the same pass.** Clearing the display name left Save enabled — `nameTooShort` required at least one character, so an empty box was not "too short" — and the click failed with the generic "That did not work. Please try again." about a rule nobody had stated; blank is now too short, and the one custom throw has a mapped code so an unmapped one cannot report itself that way again. The new password is typed twice, as sign-up asks for it, because a typo here sets a password the owner does not know and they find out at the next sign-in on another device. And failures now render `role="alert"` rather than `role="status"`: a wrong password or a deletion that did not happen is not a polite update, and `status` is announced whenever the reader gets round to it.

**The split between the two test files is deliberate.** `AccountDialog.test.tsx` mocks Firebase and proves the surface — which control is enabled when, what each sends, what it says when it fails. `tests/accountDeletion.test.ts` proves the deletion really removes the reports, their version history and the encrypted key, against the emulator with production rules. A mocked Firebase can prove neither of those, and the emulator suite cannot see a disabled button.

**Unverified email is a nudge, not a gate.** `signUpWithEmail` sends the verification mail itself so every account made through this app gets one, and the review panel shows a reminder while `!user.emailVerified` — for password accounts only, since a Google popup has already proved the address. Nothing is withheld until it is done: the account holds only that person's own reports, and locking them out of their own work would cost more than it buys.

Placeholder links are tagged **`data-needs-url`**; `grep data-needs-url src/` lists every one still waiting for a real destination. Do not "fix" them by inventing a URL. Contact and Login carry theirs in their own markup, but `SiteFooter` adds the attribute **programmatically** to any link whose `href` is a bare `#` — the maker column's placeholders were invisible to that grep until it did, which is worse than not having the convention at all. A footer column therefore needs no hand-tagging; anything else does.

**`SiteHeader` and `SiteFooter` are the single copy of the marketing chrome, and hand-editing a page's own header is how the last drift happened.** The same markup used to be pasted into all four pages; when the home page was rebuilt against the approved design the other three kept a 16px-taller bar, no backdrop blur, a 120px single-row footer instead of the four-column one, and `--secondary` (`#a04100`, a dark brown) as their accent instead of the sheet's `#fe6b00`. They now render `<SiteHeader active="…" />` and `<SiteFooter />`, so there is one copy to change. `SiteHeader` owns the profile-menu state and its click-outside handler — extracting it orphaned a `showProfileMenu` / `profileMenuRef` pair plus four imports in each of the three pages, which the unused sweep found.

`SiteFooter` takes **two** optional column lists, and each one also picks the grid ratio. `onThisPage` is the home page's five section anchors; `maker` is the contact page's maintainer profile links. `maker` → `1.6fr_1fr_1fr_1fr`, `onThisPage` → `1.7fr_1fr_1fr_1fr`, neither → three columns at `1.7fr_1fr_1fr`. Both ratios come from the approved designs for those two pages, so they are measured values rather than taste. A page that passes neither still gets exactly the three-column footer it had before, because a page with no anchorable sections should not render an empty heading. Everything else in the footer — the brand block, Product, Elsewhere and the sign-off — is the same everywhere.

**The tagline and the descriptor are different lines with different jobs, and the descriptor is duplicated into `index.html` by hand.** The *tagline* is "Show it. Build it. Ship it." — the hero `<h1>` in `LandingPage.tsx` and, in mono at `0.2em` tracking, the `SiteHeader` lockup. It works there because the hero lede sits directly underneath to explain what "show it" means. It does **not** work in a search result or a link preview, which have no lede, so the *descriptor* carries those: `<title>Forma — Screenshot to DevExpress .repx</title>` plus `description` / `og:*` / `twitter:*` in `index.html`. **The three description tags in `index.html` (`description`, `og:description`, `twitter:description`) are byte-identical to each other, and all three are a condensed form of the hero lede in `LandingPage.tsx`. Two further restatements live outside the app entirely: `description` in `package.json` — which is what npm and a GitHub repo page render — and the opening line of `README.md`. That is six places making one claim in three different lengths, so change them together.** The lede is the longer original ("a photo, a PDF, or an existing `.repx` … Three artifacts, one request."); the tags trim it to fit the ~155 characters Google renders. (Before this existed the title was a bare `Forma`, there was no description tag at all, and a pasted link rendered as a naked URL.) Three details are deliberate:

- **`og:image` is root-relative (`/logo.png`), not absolute.** Open Graph asks for absolute, but the only host value this project has is `APP_URL` in `.env`, which is **not** `VITE_`-prefixed and therefore never reaches the client — `envPrefix` allowlists `VITE_*` only, for the reasons in *The Gemini call runs in the browser* ([`gemini.md`](gemini.md)). A literal host would be wrong on every other deployment, and Vite's `%VITE_*%` HTML replacement leaves the placeholder **text** in the output when the variable is unset, shipping a visibly broken URL. Root-relative resolves against the page origin in Slack, Facebook, X and LinkedIn, and degrades to no preview image rather than a wrong one. Do not "fix" it by hardcoding a domain.
- **The preview image is `public/og-card.png`, a purpose-built 1200×630 card, and it is generated rather than drawn.** `twitter:card` is `summary_large_image` because there is now an asset with the right aspect ratio to fill it (it was `summary` while the only image was the 256×256 square mark, which the large layouts letterboxed). `og:image:width` / `:height` are declared so a scraper can lay the card out before the download finishes, and both `og:image:alt` and `twitter:image:alt` carry the same sentence.

  The source is **`assets/source/og-card.html`** — outside `public/` because anything in `public/` is copied verbatim into `dist/` and the generator is not something to ship. It is now the only tracked thing in `assets/source/`: the full-resolution logo masters that used to sit beside it were gitignored on 2026-08-29 for being half the repository, and this file is unaffected by that because it composites `../../public/logo.png`, the shipped 256px file, rather than a master. Regenerate with one command, no script and no dependencies:

  ```powershell
  & "${env:ProgramFiles(x86)}\Microsoft\Edge\Application\msedge.exe" --headless --disable-gpu --hide-scrollbars --force-device-scale-factor=1 --window-size=1200,630 --virtual-time-budget=8000 --user-data-dir="$env:TEMP\edgeshot" --screenshot="$PWD\public\og-card.png" "file:///$($PWD -replace '\\','/')/assets/source/og-card.html"
  ```

  **It must be `--headless`, not `--headless=new`** — the new headless mode ignores `--screenshot` and exits successfully having written nothing, so the old file silently survives and looks like a successful regeneration. (Verified equivalent: this one-liner and a CDP `Page.captureScreenshot` with an explicit 1200×630 clip produce **byte-identical** output, same SHA-256.)

  **Two things fail silently and yield a plausible-looking wrong card, so look at the result rather than trusting the exit code.** The card pulls Hanken Grotesk and JetBrains Mono from Google Fonts; with no network the wordmark and headline render in the fallback sans and the card is off-brand while still looking deliberate. And the logo is referenced as a `file://` sibling (`../../public/logo.png`), so a move or rename gives a blank square, not an error. The card's palette values are **literals copied from the `.landing` block** in `src/index.css`, because it renders outside the app with no access to the tokens — if the sheet's accent or ink changes there, change them here too.
- **The title is per-route, and `ROUTE_TITLES['/']` must stay byte-identical to the `<title>` in `index.html`.** `ROUTE_TITLES` is a module-scope map in **`src/lib/routes.ts`** — it lived in `App.tsx` until 2026-08-16, and this bullet went on saying so for a while after it moved, which is the drift the paragraph below now guards. `applyRoute` assigns `document.title` from `titleForRoute(path)` — one writer, at the single point every route change already passes through (boot, `navigate()`, and back/forward), so no second writer can disagree about the same tab. `index.html` still carries the `/` title because scrapers do not run this code and because it is what paints before the bundle loads; a difference between the two would rewrite the title a moment after first paint for nothing. **Change one, change both** — the same duplication rule as the theme bootstrap. Page name leads and the brand trails (`Docs — Forma`), because tab strips truncate the end. An unrecognised path falls back to `/`, matching what the route effect's `default` branch renders. `src/lib/router.ts` is deliberately still title-free: it holds routing mechanics, not copy.

**All six marketing pages carry the `landing` class on their root**, not just the home page — `LoginPage` included, which took it in the 2026-08-11 rebuild, and `LegalPage`, which took it on 2026-08-15. (Six carry `.landing`; only five mount `SiteHeader`/`SiteFooter` — `LoginPage` is the one that does not. Those are different sets, the counts in this note are not interchangeable, and both have moved twice now: check them against `src/components/` rather than trusting the number here.) It is what supplies the design's palette and the 1180px measure; without it a page falls back to the app's tokens and to `--spacing-container-max: 1440px`, and its header and footer then run wider than its own content. `DocsPage` and `FeaturesPage` also had `max-w-6xl`/`max-w-7xl` with `px-6` on their inner containers, which sat ~6px off the shared measure — those now use `max-w-container-max` / `px-margin-desktop` like everything else.

Expect to work inside `App.tsx` for workspace features; prefer extracting new surfaces into `src/components/` rather than growing it.

**The in-app `DebugConsole` was removed on 2026-08-09, and bringing it back would reintroduce a real hazard.** It was a log viewer that captured output by **monkey-patching `console.log/error/warn/debug` at boot**, and that patch was the direct cause of two bugs: `JSON.stringify` on a cyclic argument threw *at the caller's `console.log` line* rather than inside the panel, and writing to React state synchronously from inside the patched console produced "Cannot update a component while rendering a different component" whenever any library logged during render. Browser DevTools does the same job without owning the global console — `console.log` is native again, confirmed by checking `/native code/` against `String(console.log)`.

Removed with it: the `DebugLog` type, the `logs`/`isDebugOpen` state, `MAX_DEBUG_LOGS`, the patching effect, the menu entry, and the `handleRunDiagnostics`/`handleCheckSystemHealth` actions. **Nothing was lost from the telemetry**: the timing breakdown, token usage and model resolution all go through plain `console.log` in `geminiService.ts` and remain fully visible in DevTools. `elapsedTime` is unrelated and stays — it is real wall-clock around the request, shown on the progress card.

**Finding unused code:** there is no ESLint, and `tsconfig.json` enables neither `noUnusedLocals` nor `noUnusedParameters`, so dead code accumulates silently and `npm run lint` will not tell you. Run `npx tsc --noEmit --noUnusedLocals --noUnusedParameters` to see it without editing the config. Re-verified clean on 2026-09-01, so treat *any* hit as newly introduced. (It previously reported two, both in `FeaturesPage.tsx`. `heroInView` marked a visibility gate that was never wired; the looping hero animation it was meant to gate had since been reduced to static dots, so there was nothing left to gate and it went. The unused `idx` map parameter went with it. This once ended by pointing at `bentoInView` as the gate that had been missing; that went too when `.bento-grid` was deleted on 2026-08-28 as a leftover of a visual direction the landing design never adopted — see `ea37ed0` and the note in `src/index.css`. `FeaturesPage.tsx` now has no visibility gate of any kind, which is the state to compare against.)

**The workspace was rebuilt on 2026-08-12 by porting an approved artifact, and `src/workspace.css` is that port.** The brief was explicit — replicate the design pixel-for-pixel, treat the artifact as the single source of truth, do not adapt it "for consistency with the rest of the app" — so the file is arranged to keep that promise checkable rather than merely claimed:

- **The bulk of it is a verbatim block**, produced by a selector-only transform: every class gained a `wb-` prefix, and the artifact's own token, reset and `body` rules were dropped because `.sheet` and Tailwind's preflight already supply them. Every *declaration* inside it is byte-identical to the published artifact. **Do not hand-tune a value there.** Changes belong in the PREFLIGHT COMPENSATION block at the top, where they stay visible as deviations.
- **That top block is the complete list of intentional differences**, each with a comment recording what broke without it. Two matter most. Tailwind's preflight sets `line-height: inherit` on form elements, which made every pill 3px taller than the artifact's (measured 33 against 30), so `.wb-root button, input, select, textarea { line-height: normal }` puts it back. And the artifact's `.wb-shell` used `min-height: 100vh` with implicit rows, so nothing bounded the panes — `.wb-thread`, `.wb-list` and `.wb-bench-body` all declare `overflow: auto`, which needs a bounded height to do anything. That was invisible in a page whose content was fixed and happened to fill the viewport; with real content it failed in both directions, leaving a 283px dead band under the status bar when empty and pushing the bar off-screen when full. `height: 100vh` plus `grid-template-rows: minmax(0, 1fr) auto`, with `min-height: 0` on the three columns, is the fix.
- **Reduced motion is scoped to `.wb-root`.** The artifact's own rule was a global `* { transition: none !important; animation: none !important }` — harmless in a standalone page, app-wide poison in an imported stylesheet. Headless Chromium reports `prefers-reduced-motion: reduce`, so importing the file silently changed the rendering of `/docs`. **Nothing in this file may select outside `.wb-root`**, and that includes anything you add.

**The shell is a three-column grid with no top header at all** — `56px` rail, `340px` review column, `minmax(0, 1fr)` bench, and the status bar spanning `grid-column: 1 / -1` beneath all three. It replaced a header-plus-drawer arrangement, which retires several rules recorded here earlier: there is no `h-[68px]` bar to keep matched with `SiteHeader`, no `md:w-80 lg:w-96` chat pane (the column starts at the artifact's 340px, and has been resizable since 2026-08-14 — see below), and no `CanvasStatusBar` component — the bar is markup in the shell, rendered once, which was the whole point of consolidating the two copies that used to sit inside each branch. **`mobilePane` is gone as well**, with the forced flip to `canvas` on a finished result: the artifact carries no mobile tab arrangement, and inventing one would not have been a port. `.dot-grid` stays deleted, but the workspace no longer uses `.sheet-grid` either — the artifact draws its own ground.

**The review column is resizable and collapsible, and that is the first deliberate departure from the port's geometry** (2026-08-14). The artifact drew one width and offered no way to change it; with real transcripts and real file lists in that column, 340px is a guess that is wrong in both directions. Four pieces, and each of them is where it is for a reason:

- **The width is a custom property, not a rewritten rule.** `App.tsx` sets `--wb-review-w` inline on `.wb-shell`; the rules that read it are the last thing in `workspace.css` before the reduced-motion block. That is the only addition in the file that is not at the top, because the cascade leaves no choice — the ported `grid-template-columns` and the 1180px query that narrows it are both single-class selectors, so a rule in the top block loses on position, and winning on specificity instead would also beat the 880px stacked-layout rule. With the property unset both fall back to the artifact's own 340/300, so an untouched workspace still renders exactly as the port did. The full argument is in the block's own comment.
- **Collapsing has to place the bench by hand.** A `display: none` section is not a grid item, so zeroing the track was not enough: auto-placement slid `.wb-bench` up into the empty second track and drew the whole bench about 50px wide with its toolbar wrapped into a column, while the third track sat empty at 1544px. `.wb-bench { grid-column: 3 }` inside the same `min-width: 881px` query holds its place. Below 881px it must not apply at all — the shell is a single column there and an explicit column 3 would invent two implicit tracks.
- **The hide control lives in the rail, not in the panel.** It is also the only way back once the panel is gone, so it cannot sit inside what it collapses. It carries `aria-expanded` rather than `aria-current`, because that attribute is already the rail's "this section is showing" paint and this is not a section. Every other route into the column goes through `showRailPanel()`, which clears the collapse: choosing a section while the panel is hidden shows it rather than silently switching something nobody can see.
- **The width is the handle's job alone.** A widen/restore toggle briefly sat beside the hide button, jumping the column to the ceiling and back; it was removed on request the same day. Do not reintroduce it without being asked — the handle does the same thing continuously, and `End` on the handle reaches the same ceiling.
- **Removing it stranded the browsers that had used it, and that is what `REVIEW_WIDTH_EPOCH` is for.** The button wrote 1000px to `localStorage`; with the button gone, a browser that had clicked it opened to a chat column filling two thirds of the window, and the only way back was a double-click on a 7px divider nobody knows to try. Reported within the hour as "the chat width is too huge", and reproduced exactly: `56px 1000px 544px`. Persisted state that outlives the control that set it is not a preference, it is a trap. `rememberedReviewWidth()` drops the stored width once when the epoch key does not match and stamps the new one, so the column comes back to 340 on its own; a width chosen afterwards is a preference again and survives. **Bump the epoch whenever a stored width stops meaning what it meant** — it is cheaper than a support answer that ends in "double-click the divider". The ceiling came down with it, from 1000 to 560: 1000 was chosen to give the removed button somewhere worth jumping to, and nothing else ever wanted it.
- **The clamp is in `src/lib/panelSize.ts` and tested**, because both of its failure modes persist themselves: the width is written back from the same state it is read into, so a stored `4` leaves a column too narrow to contain the handle that would widen it again, and a stored `9999` pushes the bench off screen — on every reload, until someone clears `localStorage` by hand. `clampReviewWidth` also takes the viewport, which is what lets a 700px column survive a spell on a laptop and come back at 700 rather than at whatever the laptop allowed. The two keys are `reviewWidth` and `reviewCollapsed`, deliberately **not** fields in `reportConfig` — that object is an allowlist of settings describing the *document* and is read back into what the model is given (see [`persistence.md`](persistence.md)).

The handle is a focusable `role="separator"` with `aria-valuenow`/`min`/`max`: arrow keys move it (shift for a coarse step), Home and End go to the bounds, Enter collapses, and a double-click resets to 340. Verified over CDP on 2026-08-14 — drag, over-drag into the clamp, collapse, restore from a rail section, reload, a garbage stored value, keyboard, and the 800px stacked layout, which is unchanged.

**The rail is the whole navigation.** `railPanel: 'review' | 'projects' | 'history' | 'revisions' | 'batch'` chooses what the second column shows — three until 2026-09-05, when the revision history and the batch queue each took a panel rather than a modal, because both are things you watch while working on the bench rather than dialogs you open and dismiss. It and `aria-current="true"` paints the selected target. That is also why the signed-out account slot is a plain rail button and not the accent disc: the disc is simultaneously the avatar treatment *and* the selected-section treatment, so using it for "not signed in" made an empty workspace look logged in and asked for a sign-out. The mark at the top is an `<a href="/">`, matching `SiteHeader` on the five pages that mount it — the artifact drew a plain `<img>`, which left the workspace with no route back to the site. (`LoginPage` is the one `.landing` page with no `SiteHeader`; its own lockup is a bare `<Logo />` that goes nowhere, so it is not the precedent to copy here.)

**The rail expands into a labelled sidebar** (2026-08-14), 56px to 208px, remembered in `railExpanded`. Four things about it:

- **The labels are always in the markup**, hidden by `.wb-rail-label { display: none }` and revealed under `[data-rail='wide']`. Rendering them conditionally would remount the tree on every toggle, which drops focus and replays `wb-rise` for a span that costs nothing to keep.
- **The icon column does not move.** 9px of rail padding plus 9px inside each target puts the icons at x=19 against the narrow rail's centred x=18.5, and the mark's 6px margin holds it at 15 in both states. Toggling reveals text; it does not slide the icons sideways. Those two numbers are the whole reason the padding is 9 and not the 10 it was first written as.
- **The avatar does not become a full-width accent pill.** Expanded, the button goes plain and the disc moves to `.wb-initials` inside it. A stretched accent-washed row is precisely what `aria-current` paints a *selected section* as, and an account slot wearing the selected-section treatment is the confusion the signed-out slot was already fixed for once — see the paragraph above. Disc means identity, at both widths.
- **The clamp has to be told.** `panelSize.ts` reserves room for a 56px rail, so `App.tsx` hands it `viewportWidth - (railWidth - RAIL_WIDTH)`. Without that, expanding the rail and then widening the column leaves the bench 152px short of the reserve it was promised — with it, a 1600px window widened both ways comes out 208 + 972 + 420, the reserve intact.

- **The mark becomes a lockup.** Expanded, `Forma` sits beside the logo in `.wb-col-title`'s treatment — the workspace's own heading type, not `SiteHeader`'s 20px, which is sized for a 68px bar and towers over a 26px mark in a 208px rail. The word is hidden with the labels while the rail is narrow. The logo itself stays at x=15 in both states, like the icons below it.

Below 881px the expander is hidden along with the drag handle: the rail is a horizontal row there and labels in a row would overflow it. Verified over CDP on 2026-08-14 at 1600px and 800px — expand, collapse, reload, the wordmark appearing and going, and the icon column holding still across the toggle.

**The Recent panel's rows carried a bare wall-clock time until 2026-08-26**, so a session saved three weeks ago read "3:42:11 PM" directly above this morning's "9:15:04 AM" — indistinguishable, which is the whole job of that list. `formatSessionStamp()` in **`src/lib/datetime.ts`** now renders *Today, 9:15 AM* · *Yesterday, 11:05 PM* · *Aug 3, 3:42 PM* · *Aug 24, 2025, 3:42 PM*, dropping seconds (never useful for telling two sessions apart, and longer than the name above it) and adding the year only when it is not the current one.

It is a `src/lib` file with tests for one reason: **"yesterday" is a calendar question, not an arithmetic one.** Dividing a millisecond difference by 86,400,000 calls 11pm-to-1am "today" and, across a 25-hour DST day, calls a real yesterday "today" as well — right almost always, silently wrong at exactly the edges nobody re-reads. `calendarDaysAgo()` compares local midnights and rounds. Two smaller things went with it: an unreadable timestamp renders as *nothing* rather than the words "Invalid Date", and the panel's kicker no longer claims **this week** over a list that has always been every saved session regardless of age — it counts them instead. Verified over CDP against seeded history spanning all four cases plus a corrupt row.

**Recent rows can be deleted individually and in bulk** (2026-08-26). Each row carries the Projects list's hover-revealed `.wb-row-actions` trash button — same markup, same `handleDeleteReport` — and the column head gains a **Clear all** chip.

**Say the quiet part before touching this: Recent and Projects render the same `savedReports` array.** There is no separate history store, so a delete in Recent is a delete of the saved project, and signed in that is a `deleteDoc` against Firestore. "Clear all" is therefore *delete every saved project*, which is why it is a two-step in-place confirm reading "Delete all N?" rather than a single click, and why it is not labelled "clear history" — that label would describe something the app does not have. A native `confirm()` was never an option: this app removed its last `alert()` for being the one thing that broke out of its own visual language, and `CLAUDE.md` bans browser dialogs outright for freezing the extension.

Removal goes through `removeReports(ids)`, which marks the rows `wb-leaving`, waits 190ms, and only then deletes. Two details are deliberate: the fade is **opacity only** — a height collapse needs a `max-height` baseline low enough to animate from, which would clip long report names on every card that is *not* being deleted — and the leaving row is `pointer-events: none`, so a second click cannot queue the same delete twice while it fades.

The fade and the hover reveal are the second and third opacity exemptions in the reduced-motion block, on the same terms as the halo glow: no movement, no transform, and both driven by something the user just did. That is not decoration here. With the transitions killed, a destructive control appears and a row vanishes instantly under the cursor, which reads as *"did that just delete the wrong one?"* — the 190ms fade is the only feedback that the click landed where it looked. Verified over CDP under the preference: `.wb-row-actions` still reports a `0.15s` transition, and a deleted row was caught mid-fade at `opacity: 0.085`.

**The configuration dialog was refined on 2026-08-14, and four of the five changes were repairs rather than taste.** It is worth knowing which is which before touching it again.

- **A text field and a select declaring identical CSS came out 49px and 36px.** Not a bug in the rules: a `<select>` sizes from its own internal metrics, while an `<input>` under `line-height: normal` sizes from the *font's* ascent, descent and line gap, and Inter's give a 13px field a 29px line box. Setting `line-height` does not help — Chromium ignores it for the inner editing box, verified: `line-height: 1` left the field at 49, while `font-family: sans-serif` alone brought it to 35. So `input.wb-ctl, select.wb-ctl { height: 36px }` states it outright, in the PREFLIGHT COMPENSATION block, because that is exactly what that block is for: restoring the value the artifact drew against an environment that renders it differently. **This is the thing that made the form look untidy**, before any question of layout.
- **`role="dialog" aria-modal="true"` was promising a keyboard contract nothing kept.** Escape did nothing — only the attachment lightbox listened for it. Focus stayed on `document.body` when the dialog opened. Tab from *Save configuration* walked out into the rail behind the scrim. All three are fixed in one effect keyed on `isConfigOpen`, with the dismiss function reached through a ref: listing it in the deps re-runs the effect on every render, which means re-focusing the dialog on every keystroke typed into it. Focus lands on the dialog container rather than the first field, so a stray arrow key cannot change the DevExpress version before the user has read which one it is on, and returns to the rail's Configure button on close.
- **Focusing the container then painted `.wb-root :focus-visible` around all 720px of it** — the whole modal outlined in accent, which in dark mode reads as an error state. `.wb-modal:focus { outline: none }`; the ring belongs on controls the user navigated to, not on a container being handed focus programmatically. Caught in a dark-mode screenshot, not in the numbers.

**That shared `:focus-visible` rule caught the chat composer too** (fixed 2026-08-26, reported as "an orange box when I click the input"). `.wb-box:focus-within` already turns the composer pill's border accent and lifts its shadow, so the generic `outline: 2px solid var(--accent); outline-offset: 2px` drew a *second* indicator for the same focus — a hard-edged rectangle floating inside a rounded pill that had already gone orange. Note a text input matches `:focus-visible` on a plain mouse click, so this was not a keyboard-only artifact; it appeared every time anyone clicked the field. `.wb-box .wb-line:focus-visible { outline: none }` — the container takes over, which is the same trade `.wb-ctl:focus` makes in the dialog. **That is the only condition under which `outline: none` is acceptable in this file**: something else must already be showing the focus.

Screenshotting it turned up a worse one sitting underneath. `.wb-line` was a static placeholder *span* in the artifact and carried `color: var(--ink-faint)` to look like one; as a real `<input>` that rule coloured **everything the user typed** the same faint grey as the placeholder it replaced. Text is now `--ink` and the faint colour moved to `::placeholder`, where it was always meant to be. Verified in both themes: typed text `#08182b` light / `#e8eef8` dark, placeholder unchanged at `#8492a6`, pill border accent on focus and back to `--rule` at rest.
- **The key field now says `autocomplete="off"`, `spellcheck="false"`, `autocapitalize`/`autocorrect` off**, and the vault passphrases say `new-password`. The whole design of this app is that the key is never written to disk; a password manager offering to save it undoes that from outside the app, and the field is revealed as plain text by the eye button, where autocorrect would happily rewrite an API key. *Check key* is also disabled while its request is in flight and while the field is empty — it was neither, so a second click fired a second round trip to Google.
- **The one change that is taste: two columns above 760px** (`.wb-modal-col`), report on the left, key and encrypted sync on the right. That is the split the code already makes — `reportConfigStore`'s allowlist versus `keyVault` — and it takes the dialog from 468×792 in a 900px window, still scrolling, to 720×599 with nothing hidden. Under 760px it collapses back to the artifact's single column. The field order inside changed with it: version and page size share a row and the unit takes the full width, because in a 328px column the unit clipped to "Hundredths of an". The unit's *labels* are prose now while its values stay the exact DevExpress enums.

Verified over CDP on 2026-08-14, light and dark, at 1600×900, 700×900 and 1280×600: every control still drives state, Cancel and Escape both revert to the snapshot, Save persists and survives a reload, `localStorage['reportConfig']` still carries exactly the five allowlisted fields and no key, a typed key is discarded on cancel, the eye toggle still switches the field type, and Tab and Shift+Tab both wrap inside the dialog.

**The result views are derived, not stored, and there are five of them now.** The artifact had a single `plate: 'proof' | 'spec' | 'xml'` switcher where the app has two independent state variables, and the port kept the app's state rather than the artifact's shape. The switcher grew to **Mockup, Preview, Data, Spec, REPX** on 2026-09-05; the derivation did not change shape, only widen.

**It lives in `src/lib/workspaceView.ts` as of that date, and the move is the point.** `plateFor(activeTab, specView)` derives the plate and `stateForPlate(plate)` writes back to whichever of the two variables the target implies. Deriving instead of adding a third variable is what stops the switcher and the pane disagreeing — but *two* functions deriving in opposite directions can disagree with each other, and while both sat inline in `App.tsx` nothing checked that they did not. The failure is quiet in the worst way: a plate present in `plateFor` and missing from `stateForPlate` highlights its button and never opens its pane. `platesThatDoNotRoundTrip()` exists solely to be asserted on, and `workspaceView.test.ts` round-trips all five. Add a sixth plate in that file, not in `App.tsx`.

**Preview is the only pane drawn from `repxContent`, and that is its whole reason for existing (2026-09-05).** Mockup draws the `layout`, which is a picture of the *uploaded document* kept for comparing fidelity. Nothing drew the artifact the user actually downloads, so a report whose Detail band is the height of the page looked perfect right up until it opened in DevExpress. `src/lib/reportPreview.ts` reads the exported REPX back, applies the band rules — Detail once per record, PageHeader and PageFooter on every sheet, ReportHeader once, ReportFooter after the last record — and lays it out at true size. Printing goes through a `createPortal` into `document.body`, with `@page` emitted from the report's own dimensions; band tints and labels are screen affordances and are hidden in print. The `window.print()` call is deferred one frame by an 80ms timeout, because it is synchronous and blocking and the portal must have painted first.

**Editing lives on Preview, not on Mockup, and it is not a preference.** An edit on the mockup would change nothing about the exported file, and there is no reliable way to map a `layout` element back to a REPX control — they share no identifier. The preview draws the REPX itself, so the thing dragged is the thing downloaded. `src/lib/repxEdit.ts` splices the control's opening tag rather than re-serialising, so `Padding`, `StylePriority` and anything a later DevExpress version adds survive an edit untouched; the self-closing form is preserved too.

**Two render defects in the editing surface, both invisible to every check the project has.** A wrapper `div` with `inset: 0` was laid over each band to host the drag targets, which covered every *sibling* control's hit area — so in a band with more than one control only the last one drawn could be selected. Replaced with a Fragment. And a CSS comment inside a JSX template literal contained a backtick, which terminated the string early. Neither is a logic error and neither fails a test; see the component-test paragraph in `CLAUDE.md` for what a render test is and is not for.

**The Data tab's three states, and the one that shipped wrong.** It renders as: *no schema yet* (paste something), *bound already* (here is what it is bound to), or *cannot be bound* (with the reason, because the reason is usually a real finding about the report — a differing column count between the heading row and the detail row means a merged heading cell). `planDetailBinding` correctly *declines* once a report is already bound, so a second pass cannot discard work done in the designer. The pane read that decline as a failure and rendered "This report cannot be bound yet" immediately after a successful bind. The fix was ordering — check `alreadyBound` before asking whether a plan can be made — and it was verified by reintroducing the bug and watching two tests fail. That ordering is what `DataBinding.test.tsx` pins.

**Binding derives its field names once, and two derivations was the bug.** `bindFooterTotals` used to work out its own names, so moving binding into the Data tab would have emitted `sumSum([Amount])` under a detail row bound to `NET_AMOUNT`. It now takes the same optional `names` the detail pass uses. Two passes each deriving their own names agreed only while both were guessing.

**Batch is a rail panel, and the queue is a pure state machine.** `src/lib/batchQueue.ts` holds the transitions and `BatchPanel.tsx` renders them; files run **one at a time** because the user's own key carries the quota and forty parallel requests turn a slow success into a fast 429. A quota, key or permission failure stops the run rather than producing forty identical errors. **Stop** cancels what has not started and leaves the in-flight request alone, since it is already billed. The `.zip` is written by `src/lib/zip.ts` — store-only, ~150 lines, no dependency — and was verified by opening its output with .NET's own `ZipFile` reader rather than only with its own tests.

**Revisions is the fifth rail panel; its storage is not this note's business.** The panel, the structural diff and what counts as a revision are in `src/lib/revisions.ts`; everything about persisting them — the subcollection, the ten-versus-twenty cap, why signed out gets nothing, and the subcollection-delete trap — is in [`persistence.md`](persistence.md). One thing belongs here: a drag is deliberately **not** a revision. `ReportPreview` has its own undo, one entry per gesture, and pushing there as well would bury the three or four versions somebody wants under forty nudges of a label. The two mechanisms answer different questions — "take that back" and "what did this report look like before I asked for that".

**Known, and deliberately not fixed here:** `.wb-kicker` sits at 3.02:1 in light and 3.75:1 in dark, under AA's 4.5:1. It resolves to `--ink-faint`, which is app-wide and predates the port, so raising it in this file alone would put the workspace out of step with the six marketing pages over a problem all seven share.

**`SheetRuler`'s scroll marker must not paint while it is behind `SiteHeader`, and `HEADER_CLEARANCE` is what stops it.** The rule carries a live marker — a 2px `bg-secondary-container` bar with a `y 0000` chip riding it, positioned by scroll progress. At scroll 0 that puts **solid brand orange at 0,0 of the viewport**, and the header is `sticky top-0 z-50` with `backdrop-filter: blur(16px) saturate(1.5)` over `bg-surface/[0.84]` — so the header does not hide the marker, it *smears* it. Shipped that way, it painted a shapeless orange stain into the top-left corner of every page the rule renders on (all six marketing pages), on every load, before any scrolling, and was reported as "an orange background showing somewhere in all the pages" — which is exactly how an unplaceable smudge reads. **A translucent blurred header is not an occluder:** anything drawn under one has to hide itself rather than rely on being covered.

The fix is a fade, not a deletion. The marker was cut entirely on 2026-08-27 and asked for back the same day — it is the one piece of chrome on these pages that is live rather than decorative, and losing it to fix a corner artefact was the wrong trade. It now fades out below `HEADER_CLEARANCE` (76px — the header's 69px plus daylight, and there is no shared token, so a change to the header's height is a change here too) and fades back in the moment it clears. Note what that costs: on a long page the marker travels a fraction of the viewport per screen of scroll, so it stays hidden for roughly the first 8% of the page. That is the intended behaviour, not a bug to "fix" by lowering the threshold.

**The marker's readout must be derived from its position, never computed alongside it — three coordinate systems met in this one small component and two of them were wrong.** The tick labels are a *viewport* scale: a major every 96px, printed as `(y / 96) * 100`, so viewport y 384 reads "400". The marker's `top` is a *fraction of the document* mapped onto the viewport height. The chip used to be a third thing again — `(window.scrollY / 96) * 100`, the document's *absolute* scroll offset — so the number printed on the chip and the number printed on the rule beside it could only ever agree at zero. Measured across six pages at four scroll positions each: **0 of 22 agreed, worst by 8,669 units on `/docs`**, where the chip read 9,509 on a rule whose largest label is 900. Nothing threw and nothing looked broken, because every number was correct in a system the other two were not using — the same failure mode `reportGeometry.ts` exists to prevent in the report pipeline, on a different surface. `units` is now derived in the render from `top` using the *same expression the tick labels use*, which makes disagreement unrepresentable rather than merely fixed; the same measurement now reads 24 of 24 with zero drift. If you ever move it back into the scroll handler, you have reintroduced the bug.

**It cannot be reused in the workspace, and this is why.** It is `fixed inset-y-0 left-0` and driven by `window.scroll` plus `document.body.scrollHeight`. The workspace is a `height: 100vh` frame whose scrolling happens *inside* panes, so the component would pin to the viewport's left edge — over the rail, not the bench — with a marker that never moves because the window never scrolls. A static decorative copy was considered and rejected: the marketing ruler earns its place by actually reading out scroll position in report units, and a fake one on a surface whose sheet is drawn at a computed `scale` would imply a measurement it cannot make.

**`ReportMockup` is a scaled pixel sheet, not a responsive layout.** Every coordinate from `layout` is multiplied by `0.96` and set as an absolute `left/top/width/height`; the page cannot reflow, so a `ResizeObserver` measures the viewport and applies `transform: scale()` to the whole sheet, with an outer stage div sized to the post-scale footprint so nothing overlaps. Everything inside the sheet is drawn against the **theme-invariant `--paper` / `--on-paper` / `--paper-accent` / `--paper-accent-soft` tokens** — all four deliberately identical in `:root` and `.dark`, since a rendered report must look the same in both themes. Using `bg-card`/`text-foreground` or any other flipping token inside the sheet is a bug, not a shortcut. That was not hypothetical: the chart bars used `bg-primary/40` and the gauge arc `border-t-primary`, and `--primary` **does** flip (`#FF5E00` → `#FF6C1A`), so a generated report quietly changed colour with the app theme. `--paper-accent` exists so there is a correct token to reach for instead of a literal.

**Do not wrap `ReportMockup` in a frame — it draws its own.** Border, radius, shadow, max-width and the grey inset around the page are all inside the component. The canvas used to wrap it in a second panel with its own border, shadow, rounding and `bg-paper`, which stacked white-then-grey-then-white with two borders and two shadows. The call site is now just `<ReportMockup … />` inside the scrolling canvas.

**The Mockup draws a repeated heading row once, and the layout still carries it twice (2026-09-06).** The `layout`'s PageHeader section holds the column headings, and the detail table repeats them as its own first row — so the mockup drew "Item Description / Amount" twice, a few units apart, on every report of that shape. It was reported as a mockup bug and it is one, but **the duplicate in the data has to stay.** `recordCountFromLayout` counts the detail table's rows and subtracts one for the heading, so deleting the row costs a record in the Preview and says nothing; and the prompt asks the model for every row it can see, *including* the header row, because the layout describes the SOURCE document rather than the report. Editing it to suit a renderer would make the pane lie about what came back. So the rule is in the renderer: [`src/lib/mockupRows.ts`](../../src/lib/mockupRows.ts) returns the detail tables whose first row an **earlier** section already drew, keyed by section and element index, and `MockupTable` skips it. Narrow on purpose — earlier only, so a totals table below does not license dropping a row above it; cell-for-cell equality, so a heading that was never repeated stays; never the last remaining row, so a one-row table cannot render empty; and blank rows never match each other. When nothing is duplicated nothing changes, which is what makes it safe on real reports rather than only on the mock. The last case in `mockupRows.test.ts` asserts the record count is untouched, so a later "simplification" that moves this into the data fails there.

**Routing is real paths** — `/features`, `/docs`, `/contact`, `/login`, `/signup`, `/workspace`, `/terms`, `/privacy`, and `/` for the landing page. It was `window.location.hash` until 2026-08-09, which showed a `#` in the address bar on every page and a bare `/#` on the home page.

**`ROUTES` in `router.ts` is that list, and it has to be kept that way by hand.** It went stale once: `/terms` and `/privacy` shipped on 2026-08-15 answered by `App.tsx`'s route effect and `ROUTE_TITLES` alone, while `ROUTES` still stopped at `/workspace`. Nothing broke, and nothing would have — `ROUTES` is read by nothing but `migrateLegacyHashUrl()`, and both pages postdate the hash era by six days, so no `#terms` bookmark can exist to migrate. That is exactly what made it worth fixing rather than leaving: the array had no consequence to warn anyone it was wrong, so it would have kept drifting until a route that *did* have a legacy link went missing from it and silently migrated to the home page. Both are in the list as of 2026-08-16, and the const carries a comment saying what it feeds. **A new route needs three edits, not one**: `ROUTES`, its branch in the view mapping, and its title. **All three are now enforced** (2026-08-16). `src/lib/router.test.ts` covers the mechanics — that `/terms` and `/privacy` are in `ROUTES` and migrate rather than falling back to the home page, that a trailing slash never becomes a second route, and that neither `navigate({replace})` nor the migration pushes a history entry.

The other two moved out of `App.tsx` to be reachable at all. **`ROUTE_TITLES` and the route effect's `switch` now live in `src/lib/routes.ts`** as `titleForRoute()` and `viewForRoute()`, extracted for the same reason `repx.ts` was — a pure mapping cannot be tested from inside a component that imports pdf.js and Firebase. The effect is now four lines that apply what `viewForRoute` returns. Three things that pins:

- **Every route has a title, and no title exists for a non-route.** A missing one silently showed the home page's title in the tab.
- **`ROUTE_TITLES['/']` is byte-identical to the `<title>` in `index.html`.** The test reads the file off disk, so the long-documented "change one, change both" rule is checked rather than remembered.
- **Every route has a branch of its own** — `routesWithoutABranch()` must return empty. This is the silent one: a route in `ROUTES` with no branch renders the home page under its own URL, which looks like a page that works.

**`null` in `RouteView` means "leave that state alone", and it is load-bearing.** `/login` and `/signup` return `null` for `showWorkspace` so the modal renders over the workspace instead of closing it, and `null` for `lastViewPath` because a sign-in screen is not somewhere to return *to* — that second one is the bug described under *Signing in returns you where you were* in [`persistence.md`](persistence.md), now pinned by two tests. Do not "simplify" either to `false` or to the path.

`src/lib/router.ts` is the whole implementation and there is still no router library: `currentPath()`, `navigate()`, `onRouteChange()` and `migrateLegacyHashUrl()`, in a file that is mostly comment. (It said "~75 lines" here until the count reached 85, which is the same rot `CLAUDE.md` warns about for line numbers — a length is not a fact worth writing down.) Three things about it are load-bearing:

- **`pushState` fires no event.** Only the back/forward buttons emit `popstate`, so `navigate()` dispatches a custom `forma:routechange`; `onRouteChange` subscribes to both. Calling `history.pushState` directly instead of `navigate()` will move the URL without re-rendering.
- **Deep links depend on the server.** Vite runs with `appType: "spa"` in development and `server.ts` has an `app.get('*')` fallback in production, so `/features` returns index.html. On a host without that fallback, every deep link 404s — which is the reason hash routing existed in the first place.
- **Links are plain `<a href="/...">`.** One delegated click handler in `App.tsx` intercepts them, so anchors stay right-clickable and middle-clickable. Modified clicks, `target="_blank"` and downloads are left to the browser.

`migrateLegacyHashUrl()` rewrites an old `#features` bookmark to `/features` via `replaceState` (no extra history entry), strips a bare trailing `#`, and drops unknown fragments. It runs at boot *and* on `hashchange`, because a legacy link followed from inside the app is a same-document change that never remounts. Note a bare `#` reports `location.hash === ''`, so that case has to be read off `href`.

Any new page needs a branch in the route effect *and* a `setLastViewPath` update if it should be restorable after login.

Two pieces of view state shape the result pane: `activeTab: 'spec' | 'ui'` switches between the markdown spec and the `ReportMockup`, and `specView: 'spec' | 'repx'` chooses between the markdown write-up and `RepxViewer` inside the spec tab — that tab was named "Specs & REPX" from the start but rendered only the markdown until the viewer was added. Both are still the storage; the rebuilt workspace presents them through the derived `plate` described above, so read that before adding a third.

**`checkRepx` now measures the geometry as well as the structure** (2026-08-26). It used to pass anything that parsed, had the right root and contained a `<Bands>` — which is exactly why it passed the margin bug described under *The units audit* in [`gemini.md`](gemini.md), where every control was an inch out and the right-hand column fell outside the printable width. Structure was all it ever looked at.

The check reads `PageWidth`/`PageHeight`/`Margins` off the root and, per band, compares each control's `LocationFloat` + `SizeF` against the printable area and the band's own `HeightF`, plus the bands' total against the page. Warnings name the control (`"amountTotal" runs to 820 across, past the 650-unit printable width`) because a count is not actionable and the name is what makes it findable in the designer.

Three things about it are deliberate and should survive any edit:

- **A geometry problem never sets `ok: false`.** `ok` gates Export and Open in designer; a control fifty units too wide still opens perfectly well, and refusing to export it would be a worse failure than the one being reported. Warnings are a separate field, and the REPX pane gets a third badge state — neither green (which hides the finding) nor red (which says the file is broken when it is not). A test asserts the export stays enabled.
- **Only controls directly inside a band's `<Controls>` are measured.** Anything nested inside another control is positioned relative to *that* parent, so measuring it against the page would invent failures; table rows and cells carry `Weight` and no `LocationFloat` at all. Checking less and being right is the trade.
- **No page size means no warnings.** Nothing to measure against is not the same as everything being wrong.

`RepxViewer` is dependency-free, and its three pure helpers now live in **`src/lib/repx.ts`** (moved out of `App.tsx` so they could be unit-tested without importing the app, pdf.js and Firebase into a test run — `src/lib/repx.test.ts` covers them): `formatXml()` re-indents (safe here only because DevExpress REPX is attribute-based with no meaningful text nodes — do not reuse it for general XML), `tokenizeXml()` returns **tokens rather than an HTML string** so the XML renders as React text nodes and can never be injected as markup, and `checkRepx()` runs a `DOMParser` pass asserting the document parses, that the root is `XtraReportsLayoutSerializer`, and that `<Bands>` exists. That check is the only thing standing between a malformed generation and the user discovering it when DevExpress refuses the file. `copyText()` stays in `App.tsx` (it touches the DOM directly) and falls back to a hidden textarea plus `execCommand` because `navigator.clipboard` is undefined outside a secure context — the same LAN-over-HTTP case that `isVaultAvailable()` guards (*Where the user's key lives* in [`gemini.md`](gemini.md)).

**The REPX pane is dark in *both* themes**, on `--code-bg` / `--code-rule` / `--code-ink` / `--code-ink-faint`, matching the code blocks on the docs and features pages. It used to be white in light mode with a hardcoded `dark:bg-[#0d1117]` twin. The consequence is that **nothing inside that pane may use a token that flips**: `XML_TOKEN_CLASS` therefore carries the former dark-mode syntax colours *unconditionally* (no `dark:` variants), and the line-number gutter and row hover are fixed values. Adding a `dark:` variant back would light the text up against a background that never lightened.

**File intake is now a single path.** `handleFileChange` (picker) and `handleDrop` (drag-and-drop) both call `ingestFiles()`, which loops `ingestFile()` per file. These were two near-identical copies that had to be edited in lockstep; that duplication is gone and must not come back. (A parallel `files: File[]` state also used to exist; it was write-only — set in four places, read nowhere — and has been removed.)

Intake produces **two** outputs, deliberately kept apart:

- `previews: string[]` — image data URLs only. Everything downstream depends on this staying image-only: the thumbnail strip, `sourceRect` logo cropping, and the `images` field persisted on saved reports.
- `attachmentTexts: TextAttachment[]` — text recovered *from the files themselves*. Each entry carries the **same `uploadId` as its page images**, plus `file` and an optional `page`; display is derived from those and never stored pre-formatted.

**The text side is where `.repx` accuracy comes from.** A digital PDF already contains every string and its exact position; rasterizing and asking the model to read it back out of pixels throws that away. `extractPdfPageText()` pulls it via `page.getTextContent()` and converts it into the report's own grid — **hundredths of an inch, origin top-left** — so the model can use the numbers directly. Two conversions matter, and nothing checks either one: PDF points are 1/72" (`PT_TO_REPORT_UNITS = 100/72`, which maps a 612×792pt Letter page exactly onto the 850×1100 report grid), and PDF y is a **baseline measured from the bottom**, so `yTop = pageHeight - (baselineY + height)`. Get either wrong and every coordinate is silently off.

`MAX_TEXT_ITEMS_PER_PAGE` (400) caps extracted strings per page so a dense page cannot flood the prompt — and unlike the page cap, **this one is silent**: it `break`s out of the loop with no `uploadNotices` entry, so a very text-heavy page loses its tail with nothing surfaced to the user. If accuracy is missing from the *bottom* of a busy page, check this before blaming the model.

The prompt's **SOURCE PRECEDENCE** block tells the model this extracted text is exact and outranks the page image for strings and positions, while the image remains authoritative for borders, fills, logos and visual structure. Keep those two halves in sync.

- **PDF**: up to `MAX_PDF_PAGES` (8) pages, each rendered at `PDF_RENDER_SCALE` (2.5, ~250 DPI) *and* text-extracted. Pages beyond the cap produce a user-visible notice rather than vanishing — page 1 used to be the silent hard limit. The canvas is **filled white before rendering** (pdf.js draws onto transparency; JPEG has no alpha, so skipping this turns the page black) and the result goes through `optimizeImageDataUrl()`, which matters now that 2.5 can exceed the edge cap. A PDF with no text layer is a scan: that is detected and reported, not treated as a failure.
- **Images**: `readAsDataUrl()` then `optimizeImageDataUrl()`, which caps the long edge at `MAX_IMAGE_EDGE` (2048) and re-encodes as JPEG. Anything already under both that cap and `REENCODE_THRESHOLD_BYTES` is untouched; it returns the original on any failure and never returns a larger payload than it was given, so it can only help. The cap is deliberately generous — lowering it trades real fidelity for speed.
- **`.repx`**: read with `file.text()` as a real string, validated with `checkRepx()` (the same check the REPX viewer uses) and attached as text. It used to be base64-relabelled `text/plain` and never decoded — 33% larger for no benefit, and impossible to validate before spending a request.

**One dropped file is one chip, and until 2026-08-26 that was only half true.** The images collapse through `groupAttachments` and always did; the text layer did not, so a three-page PDF drew one thumbnail chip *and three more* reading "invoice.pdf · page 1 text", "· page 2 text", "· page 3 text". That is the complaint the grouping exists to prevent, arriving through the door nobody had closed — the user sees the product having torn their file into pages. Text from an upload that produced images is now part of that upload's chip and is not listed again (`textGroups` filters on the `uploadId`s present in `previewMeta`); what remains in the second row is genuinely text-only — a `.repx`, or a PDF whose pages all failed to rasterise — and it is grouped by the same rule so the defect cannot re-open from that side. Verified over CDP: a 3-page PDF yields exactly one chip carrying a "3 pages" badge, and a `.repx` still yields its own.

Two consequences of the same confusion went with it, and both were worse than the chips:

- **`removeFile` dropped the images and left the text behind.** Once the text had no chip of its own that was unobservable — a PDF the user had visibly taken back was still being sent to the model, with nothing on screen left to remove it with. It now takes the `uploadId` and clears both lists. (The natural check is the good one: remove the chip and the text row must not appear, since with the images gone nothing would suppress it any more.)
- **`MAX_ATTACHMENTS` (12) counted rows, not files.** An 8-page PDF spent 8 image slots plus up to 8 text slots, so a single upload could overrun a cap the notice describes in files — "you can attach up to 12 items", said to someone who attached one. `stagedUploads` counts distinct `uploadId`s instead. Five 3-page PDFs (15 images + 15 texts) now stage without a notice, which is the case that used to fail at the second one.

Unsupported files, oversized files (`MAX_FILE_BYTES`), and the `MAX_ATTACHMENTS` cap all surface through `uploadNotices`. Dropping a `.docx` previously matched no branch and did nothing at all, with no feedback.

**Attachments are not always images.** `analyzeReportDesign` takes `AttachmentPart[]` — a union of `{inlineData}` and `{text}` — and text parts are ordered **first** so exact strings are in context before the page images. Anything that reasons over attachment counts must consider both lists: `handleGenerate`'s chat-vs-report routing, its empty guard, and the send button all check `previews.length === 0 && attachmentTexts.length === 0`. A `.repx`-only upload has no images at all, and checking `previews` alone would wrongly route it to chat.

pdf.js is wired at module scope with a Vite-specific import — `import pdfWorker from 'pdfjs-dist/build/pdf.worker.mjs?url'` assigned to `pdfjs.GlobalWorkerOptions.workerSrc`. The `?url` suffix is what keeps the worker out of the main bundle; swapping it for a CDN string or a bare import breaks PDF intake at runtime, not at build time.

`previews` are split back into `{inlineData: {data, mimeType}}` parts at request time inside both `handleGenerate` and `handleResume` — duplicated again, once for the initial run and once for resume.

**Export** goes through `downloadDesign`: it prefers `result.repxContent` (Blob as `application/xml`, filename `<title>.repx`) and silently degrades to `result.content` as `.txt` when the model returned no XML. The `.repx` filename is the report title with whitespace collapsed to underscores.

**Progress is real once output starts; cancellation is real throughout.** The request is streamed — `analyzeReportDesign` calls `ai.models.generateContentStream` and accumulates chunks, taking an optional 6th argument `onProgress` that fires (throttled to ~120ms) with `{chars, percent, elapsedMs}`. The response is a single schema-constrained JSON object, so it still cannot be *parsed* until the last chunk lands; what streaming buys is a progress bar driven by output actually received rather than a timer inventing percentages. `percent` is asymptotic (`1 - e^(-chars/EXPECTED_RESPONSE_CHARS)`) and deliberately caps at 95 — only the parsed result reaches 100, so the bar can never claim completion early. `EXPECTED_RESPONSE_CHARS` only shapes the curve; being wrong makes the bar move faster or slower, never wrong.

**The bar became a ring on 2026-08-26, and the card lost two thirds of its furniture.** It had grown to five stacked rows — a spinning icon and a clock, a track, a stage line, three shimmering skeleton bars, and an explanation — sitting in the middle of a conversation to announce a job the user had just started themselves. `LogoPulse.tsx` now draws the Forma mark inside two concentric rings, and **the inner one is the bar**: same `analyzingProgress`, same 95% cap, bent into a circle. The outer orbit is decoration and is drawn deliberately differently (thinner, dashed, wider radius) so it cannot be mistaken for a second measurement. What was deleted was duplicated elsewhere: the workspace's own status bar already carries the state pip and the elapsed clock, three lines of skeleton were standing in for a bar that was right there, and `.wb-spin` / `.wb-track` / `.wb-skeleton` / `wb-sweep` went with them (each had exactly one call site).

**The reduced-motion handling is the load-bearing part, and it is not the obvious one.** `workspace.css` ends with a blanket `.wb-root * { animation: none !important }`, so every keyframe in that card — including the spinner it replaced — is dead for anyone with the preference set, which on **Windows Server is the default**, i.e. the machine this is developed on. The design is therefore built so nothing important is carried by motion: the arc advances because output arrived rather than because a keyframe looped, so under the preference it snaps to each new value instead of easing, and still moves. One exception is re-enabled by name in that block: a slow opacity breathe on a blurred disc behind the mark, chosen because it changes luminance and nothing else — no translation, rotation or scale — and because a card that can sit for two minutes with an honestly-pinned ring is otherwise indistinguishable from a crash. The orbit is *not* exempted: it rotates, and it means nothing. Verified over CDP by reading `animationName`/`animationPlayState` under both media values — `no-preference` gives breathe + orbit running and a 0.25s arc transition, `reduce` gives breathe running, orbit `off`, transition `0s`.

The card's geometry is set by the review panel, not the window: at its 340px default the first attempt wrapped the heading and cut the stage sentence to "Extracting layout…". The sentence now spans the controls' column and is clamped to two lines at a **fixed** height, so the card cannot change size as the stage text changes and jog the transcript beneath it. Measure any change to it at 340px.

`App.tsx` still starts the old 2.5s `setInterval` because model detection and the upload genuinely produce no signal. `handleStreamProgress` **clears that interval on its first call** and takes over — both handlers share the one callback so they cannot drift.

**The bar can never go backwards, and two mechanisms are what guarantee it.** `PRE_STREAM_CEILING` (14) clamps everything the simulated interval may claim, so the pre-stream phase cannot outrun what the stream will report on its first real chunk — without it the interval could sit at 30% while the first chunk honestly said 6%, and the bar would visibly retreat. `Math.max` against the previous value is the second half: a lower percent is discarded rather than rendered. `handleStreamProgress` then floors the stream at `PRE_STREAM_CEILING + 1` so the handover is always a step forward. Both are shared by `handleGenerate` and `handleResume` for the same anti-drift reason as the callback itself. If you add any new progress writer, route it through `src/lib/generationProgress.ts` — calling `setProgress` with a bare object is the bug.

**All of the above moved out of `App.tsx` and this paragraph did not follow it for a while, so mind the names.** The rules now live in [`src/lib/generationProgress.ts`](../../src/lib/generationProgress.ts) under test (22 cases), and the state is a single `GenerationProgress` object rather than a loose percent. The mapping from the old names, which no longer exist anywhere in the source: `advanceProgress` → the `Math.max(state.percent, …)` inside `tickSimulated` and `applyStreamProgress`; `setAnalyzingProgress` → `setProgress`; `analyzingProgress` → `progress`. `PRE_STREAM_CEILING` survived the move unchanged and is exported from that module. What is left in `App.tsx` is wiring: stop the interval, fold the chunk in — its own doc comment above `handleStreamProgress` says so. The `finally` blocks and `handlePause`/`handleStop` all null-check `loadingIntervalRef.current`, which is required now that the stream nulls it mid-flight. Pause and stop abort a per-run `AbortController` whose `signal` is threaded into `analyzeReportDesign` as the 5th argument and handed to the SDK via `config.abortSignal`, tearing down the in-flight fetch. Two consequences worth knowing: the SDK's abort is **client-side only**, so Google may still finish and bill for the generation; and a cancelled call rejects with a `DOMException` named `AbortError` that `geminiService.ts` deliberately rethrows unwrapped, so callers can distinguish cancellation from a real model error. Both `handleGenerate` and `handleResume` guard on `signal.aborted` in their `catch` and `finally` blocks — pause leaves `isAnalyzing` true so the resume path can re-issue the request, while `handleStop` resets the workspace itself.

**The REPX audit is a count in the status bar, and it blocks nothing.** [`src/lib/repxAudit.ts`](../../src/lib/repxAudit.ts) asks what is still wrong with a report *after* every repair pass has run, and it exists because on 2026-09-04 three defects reached the user while the app reported success: the tables were discarded on load, the page numbering was dropped, and the report had no Detail band. By its own lights it had succeeded — the XML parsed, the mockup drew, the spec read well. **Do not restate the checks here.** Why each one exists is that file's own doc comment, and how each was measured against DevExpress is in [`gemini.md`](gemini.md), where the measurement sections keep ending "`repxAudit.ts` gained two checks from this". This paragraph owns only the part neither of them covers: where it runs, and what the user is shown.

**It runs in three places, and only one of them is visible.** `geminiService.ts` calls it last on the finished artifact and writes the result to the **console** — `console.error` / `warn` / `log` by severity, then every finding as `[code] message`. `App.tsx`'s `runOneBatchFile` calls it per batch file so a row can carry its own `errors`/`warnings` counts, which is the point of running forty files: knowing which of the forty are worth opening. And a `useMemo` over `result.repxContent` and `result.layout` produces the one the user sees.

**What the user sees is a count and nothing else.** One `<span>` in the workspace status bar, beside the bound-fields readout, **silent when the audit is clean** — which is the common case, and the reason it can sit in a bar rather than in a panel. `wb-bad` if there is any error, `wb-warn` otherwise, reading "2 REPX errors, 1 REPX warning". The full text of every finding is in the `title` tooltip and in the console; the bar has room for a number. Findings carry a stable `code` precisely so a future caller can filter on it without matching prose, and nothing does that yet.

**Nothing is gated on it, and that is deliberate rather than unfinished.** A warning means the file opens and is a worse report than it should be, which is the user's call to make — they asked for a report and got one. An error means a repair *declined*, which is worth knowing and still not worth withholding the file over. If you are tempted to block export on `audit.errors`, the argument against is in the file's *Errors versus warnings* comment; the argument for would have to be a new one.

**The `layout` argument is what makes the vaguest finding the most specific**, and it is the reason the audit takes two artifacts instead of one. "There is no Detail band" is true of a certificate, where it is correct. "The mockup drew a 12-row grid and there is no Detail band" is a defect. The two artifacts come out of one response and describe one document, so where they disagree one of them is wrong — and that is checkable in a way neither is alone. It is also why the band-count mismatch is worth reporting at all: the band count shown in the UI comes from the `layout`, while the file the user exports is the REPX. The argument is optional, because a report loaded from an old save may not have one and the structural checks are worth running either way.

At 68 cases `repxAudit.test.ts` is the second-largest file in the unit suite, behind `reportPreview.test.ts` — see [`testing.md`](testing.md).
